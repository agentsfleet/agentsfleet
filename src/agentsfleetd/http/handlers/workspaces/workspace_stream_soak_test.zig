// Chaos + soak for the multiplexed workspace SSE stream. Populated after the
// handler compiles.

const std = @import("std");

test "placeholder: workspace stream soak suite compiles" {
    try std.testing.expect(true);
}
