{
  "openapi": "3.1.0",
  "info": {
    "title": "agentsfleet Control Plane API",
    "description": "API for managing workspaces, fleets, triggers, and runs.",
    "contact": {
      "name": "agentsfleet",
      "url": "https://agentsfleet.net"
    },
    "license": {
      "name": "MIT",
      "identifier": "MIT"
    },
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "https://api.agentsfleet.net",
      "description": "Production"
    }
  ],
  "paths": {
    "/healthz": {
      "get": {
        "tags": [
          "Health"
        ],
        "summary": "Liveness check",
        "description": "Returns 200 as long as the process is alive and able to serve HTTP. Does not probe dependencies — use /readyz for readiness (database + queue). ",
        "operationId": "healthz",
        "responses": {
          "200": {
            "description": "The process is alive"
          }
        },
        "security": []
      }
    },
    "/readyz": {
      "get": {
        "tags": [
          "Health"
        ],
        "summary": "Readiness probe",
        "description": "Returns 200 when the database and event queue are available, and 503 when either is not. Never shed: an orchestrator that cannot reach this because the instance is busy learns nothing it can act on. ",
        "operationId": "readyz",
        "responses": {
          "200": {
            "description": "Every dependency this instance needs is reachable"
          },
          "503": {
            "description": "A dependency this instance needs is unreachable"
          }
        },
        "security": []
      }
    },
    "/v1/admin/fleet-libraries": {
      "get": {
        "tags": [
          "Fleet library"
        ],
        "summary": "List the platform Fleet library catalog",
        "description": "Lists every entry in the global platform catalog. Published, draft, and entries whose bundle was never fetched all appear. Unlike the workspace gallery, this operator view hides nothing: it shows what is live and what still needs work. Requires the `platform- library:write` scope. Metadata only — never bundle markdown, a support-file body, or an object-store key. Each row carries an `etag` that an editor can send as `If-Match` on PATCH. ",
        "operationId": "list_platform_fleet_library",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminLibrariesResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "post": {
        "tags": [
          "Fleet library"
        ],
        "summary": "Onboard a platform Fleet library entry",
        "description": "Onboards a Fleet library entry into the global platform catalog from a GitHub source reference. Requires the `platform-library:write` scope. The canonical bundle is written to internal object storage keyed by content hash; the response carries metadata only — never an object-store key or support-file content. ",
        "operationId": "onboard_platform_fleet_library",
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminLibraryCreated"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "413": {
            "description": "The payload is over this route's ceiling"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/admin/fleet-libraries/{id}": {
      "delete": {
        "tags": [
          "Fleet library"
        ],
        "summary": "Delete an unpublished platform Fleet library entry",
        "description": "Removes a catalog entry. Only an entry that is NOT published may be deleted. A live fleet is never taken away from the tenants who can install it, so unpublish it first. Workspaces that already installed the fleet are unaffected: an install snapshots the bundle, so it keeps running. Requires the `platform-library:write` scope. ",
        "operationId": "delete_platform_fleet_library",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "`UUIDv7` of the resource.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "patch": {
        "tags": [
          "Fleet library"
        ],
        "summary": "Curate, publish, or unpublish a platform Fleet library entry",
        "description": "Partial update. `description` and `required_credentials_reasons` are the two fields no bundle can supply, so they are operator-owned: a later bundle refetch never overwrites them. `published` moves the entry between `draft` (stored, invisible to every tenant) and `public` (live in every workspace gallery and installable). Publishing an entry whose bundle was never fetched is refused — a published entry always has something to install. Requires the `platform-library:write` scope. Send `If-Match` with the row's `etag` to reject stale edits before they can repoint the source or unpublish the entry. Omitting the header preserves last-write-wins behavior. ",
        "operationId": "update_platform_fleet_library",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "`UUIDv7` of the resource.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "If-Match",
            "in": "header",
            "description": "Optional catalog row version from the list response. Stale values return 412 with the current `etag`.",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "412": {
            "description": "The precondition the caller supplied does not hold"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/admin/models": {
      "get": {
        "tags": [
          "Admin"
        ],
        "summary": "List models",
        "description": "Returns every shared model, ordered by provider and model identifier. Requires the `model:read` scope. ",
        "operationId": "list_admin_models",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminModelsResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "post": {
        "tags": [
          "Admin"
        ],
        "summary": "Add a priced model to the catalogue",
        "description": "Creates one catalogue row. `(provider, model_id)` is the row's immutable identity — change either by deleting and re-adding. The rate cache is repopulated on success, so a new price is live with no restart. Requires the `model:admin` scope. ",
        "operationId": "create_admin_model",
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminModelCreated"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/admin/models/{id}": {
      "delete": {
        "tags": [
          "Admin"
        ],
        "summary": "Remove a model",
        "description": "Refuses to remove the active default model. Choose another default first. Requires the `model:admin` scope. ",
        "operationId": "delete_admin_model",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "`UUIDv7` of the resource.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "patch": {
        "tags": [
          "Admin"
        ],
        "summary": "Update a catalogue row's caps and rates",
        "description": "Rates-only change; `provider` and `model_id` are the row identity and are immutable on this endpoint. The rate cache is repopulated on success. Requires the `model:admin` scope. ",
        "operationId": "update_admin_model",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "`UUIDv7` of the resource.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminModelUpdated"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/admin/platform-keys": {
      "get": {
        "tags": [
          "Admin"
        ],
        "summary": "List platform LLM keys",
        "description": "Lists all configured platform-wide LLM provider keys. These are shared keys that workspaces fall back to when no self-managed key is configured. ",
        "operationId": "list_platform_keys",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlatformKeysResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "put": {
        "tags": [
          "Admin"
        ],
        "summary": "Set a platform LLM key",
        "description": "Configures a platform-wide LLM provider key by referencing a workspace that has a self-managed key stored. The platform key proxies through the referenced workspace's credential. ",
        "operationId": "put_platform_key",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlatformKeySetResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/admin/platform-keys/{provider}": {
      "delete": {
        "tags": [
          "Admin"
        ],
        "summary": "Delete a platform LLM key",
        "description": "Removes a platform-wide LLM provider key. Workspaces without self- managed keys will no longer be able to use this provider. ",
        "operationId": "delete_platform_key",
        "parameters": [
          {
            "name": "provider",
            "in": "path",
            "description": "Provider identifier, such as `slack` or `github`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlatformKeyDeactivateResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/api-keys": {
      "get": {
        "tags": [
          "API Keys"
        ],
        "summary": "List tenant API keys",
        "description": "Returns metadata for every tenant API key visible to the caller, under Stripe-style keyset pagination. The cursor carries the boundary sort value, so every allowlisted sort pages without loss. The retired page and page_size parameters are refused. Results are always scoped to the caller's tenant; `key_hash` is never returned. ",
        "operationId": "list_api_keys",
        "parameters": [
          {
            "name": "starting_after",
            "in": "query",
            "description": "An opaque cursor from a previous page's `next_cursor`, issued under the same sort.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sort",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PageResponse_ApiKeySummary"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "post": {
        "tags": [
          "API Keys"
        ],
        "summary": "Mint a tenant API key",
        "description": "Creates a tenant admin key with the `agt_t` prefix. The raw key is returned once. Only its SHA-256 hash is saved. ",
        "operationId": "create_api_key",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MintApiKeyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MintedApiKeyResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/api-keys/{id}": {
      "delete": {
        "tags": [
          "API Keys"
        ],
        "summary": "Delete a revoked tenant API key",
        "description": "Deletes a revoked tenant API key. An active key returns 409 `UZ- APIKEY-008`. ",
        "operationId": "delete_api_key",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "`UUIDv7` of the resource.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "patch": {
        "tags": [
          "API Keys"
        ],
        "summary": "Revoke a tenant API key",
        "description": "Partial lifecycle update. Body must be `{\"active\": false}` — re- activation is not supported; mint a new key instead. Sets `revoked_at` and `updated_at`. ",
        "operationId": "revoke_api_key",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "`UUIDv7` of the resource.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PatchApiKeyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RevokedApiKeyResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/auth/identity-events/clerk": {
      "post": {
        "tags": [
          "auth-identity-events"
        ],
        "summary": "Receive a Clerk account event",
        "description": "Receives signed account events from Clerk. A `user.created` event creates the user's account and default workspace. Repeated events return `created:false`. Unsupported event types return 200 with `status: ignored`. ",
        "operationId": "auth_identity_event_clerk",
        "parameters": [
          {
            "name": "svix-id",
            "in": "header",
            "description": "Svix message identifier (used for deduplication and signature binding).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "svix-timestamp",
            "in": "header",
            "description": "Unix timestamp of the Svix delivery (used for replay protection).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "svix-signature",
            "in": "header",
            "description": "Space-separated list of Svix v1 signatures.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Ignored"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "413": {
            "description": "The payload is over this route's ceiling"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": []
      }
    },
    "/v1/auth/sessions": {
      "post": {
        "tags": [
          "Authentication"
        ],
        "summary": "Start a command-line login session",
        "description": "Starts a command-line sign-in session that lasts 5 minutes. The response includes a URL for approval in a browser. No access token is required. Rate limits may return 429. ",
        "operationId": "create_auth_session",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/OpenSessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OpenSessionResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "429": {
            "description": "The instance is at its ceiling"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": []
      }
    },
    "/v1/auth/sessions/all": {
      "delete": {
        "tags": [
          "Authentication"
        ],
        "summary": "Bulk-abort every in-flight login session for the caller",
        "description": "Caller-scoped bulk delete. Enumerates every session with the caller's `clerk_user_id` whose status is `pending` or `verification_pending` and transitions each to `aborted` with `reason=\"explicit_cancel\"`. Does NOT revoke already-minted JWTs — Clerk revocation is a separate problem; an active CLI continues to work until its short-lived JWT expires. This endpoint clears in- flight login sessions only. ",
        "operationId": "delete_all_auth_sessions",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeleteAllSessionsResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/auth/sessions/{session_id}": {
      "get": {
        "tags": [
          "Authentication"
        ],
        "summary": "Read a sign-in session",
        "description": "Returns the current sign-in status. No access token is required. Expired, used, or cancelled sessions return 410 with a stable error code. ",
        "operationId": "poll_auth_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "`UUIDv7` of the login session.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PollSessionResponse"
                }
              }
            }
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "410": {
            "description": "The resource is gone and will not return"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": []
      },
      "delete": {
        "tags": [
          "Authentication"
        ],
        "summary": "Explicit cancel of a single login session",
        "description": "Used by the dashboard's \"cancel this login\" button and by the future sessions surface. Transitions the named session to `aborted` with `reason=\"explicit_cancel\"`. The Clerk JWT MUST match the session's `clerk_user_id` (set on `PATCH /v1/auth/sessions/{id}/approve`); otherwise 403. ",
        "operationId": "delete_auth_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "`UUIDv7` of the login session.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/auth/sessions/{session_id}/approve": {
      "patch": {
        "tags": [
          "Authentication"
        ],
        "summary": "Approve a sign-in session",
        "description": "Approves a pending command-line sign-in session. The dashboard sends encrypted sign-in data and a six-digit verification code. A second approval for the same session returns 409 `UZ-AUTH-015`. ",
        "operationId": "approve_auth_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "`UUIDv7` of the login session.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ApproveSessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApproveSessionResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "410": {
            "description": "The resource is gone and will not return"
          },
          "429": {
            "description": "The instance is at its ceiling"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/auth/sessions/{session_id}/verify": {
      "post": {
        "tags": [
          "Authentication"
        ],
        "summary": "Verify a sign-in session",
        "description": "Checks the six-digit code and returns encrypted sign-in data. The code can be used once. A retry from the same client within 60 seconds returns the same response. Later retries return 410 `UZ- AUTH-012`. `UZ-AUTH-011` means the code did not match. The fifth failed attempt ends the session. `UZ-AUTH-018` means the code was not six digits. `UZ-AUTH-013` means the session ended. `UZ-AUTH-014` means approval is pending. `UZ-AUTH-006` means the session expired after 5 minutes. ",
        "operationId": "verify_auth_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "`UUIDv7` of the login session.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VerifySessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VerifySessionResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "410": {
            "description": "The resource is gone and will not return"
          },
          "429": {
            "description": "The instance is at its ceiling"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": []
      }
    },
    "/v1/cli-credentials": {
      "post": {
        "tags": [
          "CLI Credentials"
        ],
        "summary": "Mint a command-line credential",
        "description": "Creates a durable credential for one machine, with the `afc_` prefix. The raw credential is returned once. Only its SHA-256 hash is saved, so it cannot be read back later. A credential belongs to a person, not to a team. Call this with a browser session token or with an existing command-line credential. A tenant API key is refused. Minting revokes whatever credential the same machine name already held. One machine holds one live credential at a time. ",
        "operationId": "create_cli_credential",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MintCliCredentialRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MintedCliCredentialResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/cli-credentials/{id}": {
      "delete": {
        "tags": [
          "CLI Credentials"
        ],
        "summary": "Revoke a command-line credential",
        "description": "Retires one of your credentials. The terminal holding it is refused on its next call, with the code `UZ-AUTH-023`. Running `agentsfleet logout` calls this for the credential on that machine. Browser sessions are untouched, so signing out of a terminal does not sign you out of the dashboard. ",
        "operationId": "revoke_cli_credential",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "`UUIDv7` of the resource.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/connectors/{provider}/callback": {
      "get": {
        "tags": [
          "Connectors"
        ],
        "summary": "Relay a provider callback to the dashboard",
        "description": "Compatibility URL for provider registrations that still target the API host. It forwards the browser to the dashboard callback with a fixed legacy marker. The authenticated endpoint uses that marker only to echo the old redirect URL during token exchange. This endpoint never exchanges a provider code or changes connector data. New provider registrations use `https://<APP_HOST>/api/connectors/{provider}/callback` for the matching environment. The dashboard posts the current Bearer token to the authenticated callback completion method. ",
        "operationId": "connector_callback",
        "parameters": [
          {
            "name": "provider",
            "in": "path",
            "description": "Provider identifier, such as `slack` or `github`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "state",
            "in": "query",
            "description": "Signed one-time value supplied through the connection URL.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "code",
            "in": "query",
            "description": "Authorization code supplied by the provider when required.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "installation_id",
            "in": "query",
            "description": "Installation identifier supplied by providers such as GitHub.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "location",
            "in": "query",
            "description": "Provider data-center location supplied by multi-region providers such as Zoho.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "302": {
            "description": "Redirect back to the caller that began the flow"
          },
          "400": {
            "description": "The request could not be read"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": []
      },
      "post": {
        "tags": [
          "Connectors"
        ],
        "summary": "Complete a provider connection",
        "description": "The dashboard calls this endpoint after the provider returns to the browser. The caller needs `connector:write` and must be the same person who started the signed connection state. The signed state binds the workspace, a keyed tag of the starter identity, a nonce, and expiry. The endpoint verifies identity and workspace access before consuming the nonce or exchanging a provider code. ",
        "operationId": "connector_callback_complete",
        "parameters": [
          {
            "name": "provider",
            "in": "path",
            "description": "Provider identifier, such as `slack` or `github`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "state",
            "in": "query",
            "description": "Signed one-time value supplied through the connection URL.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "code",
            "in": "query",
            "description": "Authorization code supplied by the provider when required.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "installation_id",
            "in": "query",
            "description": "Installation identifier supplied by providers such as GitHub.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "location",
            "in": "query",
            "description": "Provider data-center location supplied by multi-region providers such as Zoho.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "callback_source",
            "in": "query",
            "description": "Fixed compatibility marker added by the legacy API relay. Browser callers omit it.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "302": {
            "description": "Redirect back to the caller that began the flow"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/connectors/{provider}/events": {
      "post": {
        "tags": [
          "Connectors"
        ],
        "summary": "Receive Slack events",
        "description": "Slack sends signed events to this route. Users do not call this route. An invalid signature returns 401 `UZ-SLK-010`. A timestamp outside 5 minutes returns 401 `UZ-SLK-011`. ",
        "operationId": "slack_events",
        "parameters": [
          {
            "name": "provider",
            "in": "path",
            "description": "Provider identifier, such as `slack` or `github`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "X-Slack-Signature",
            "in": "header",
            "description": "Slack request signature. Slack supplies this value.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "X-Slack-Request-Timestamp",
            "in": "header",
            "description": "Delivery time in Unix seconds. Values outside 5 minutes are rejected.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "413": {
            "description": "The payload is over this route's ceiling"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/fleets/bundles": {
      "get": {
        "tags": [
          "Fleet Bundles"
        ],
        "summary": "List the platform Fleet library catalog",
        "description": "Returns the first-party (platform) Fleet library catalog. Rows contain only metadata and requirement hints; they carry no credential values or object-store keys. The workspace gallery union lives at GET /v1/workspaces/{workspace_id}/fleet-libraries. ",
        "operationId": "list_fleet_bundles",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FleetBundlesResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/fleets/runners": {
      "get": {
        "tags": [
          "Fleet"
        ],
        "summary": "List fleet runners",
        "description": "Platform-admin operator-plane read of the whole fleet, newest first over the composite (created_at, id) key. Under keyset pagination a runner enrolled mid-traversal never repeats or hides a row. The retired page, page_size and sort parameters are refused. Each row carries a derived `liveness` — never the stored auth state, never the token hash. ",
        "operationId": "list_fleet_runners",
        "parameters": [
          {
            "name": "starting_after",
            "in": "query",
            "description": "An opaque cursor from a previous page's `next_cursor`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Rows per page (1-100).",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunnersResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/fleets/runners/{runner_id}": {
      "get": {
        "tags": [
          "Fleet"
        ],
        "summary": "Get a fleet runner",
        "description": "Platform-admin read of a single runner. Carries the summary fields, a live-work snapshot, and lifetime counters from durable lease and event rows — never from in-memory metrics. The runner detail page loads from this read. ",
        "operationId": "get_fleet_runner",
        "parameters": [
          {
            "name": "runner_id",
            "in": "path",
            "description": "`UUIDv7` of the runner.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunnerDetail"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "patch": {
        "tags": [
          "Fleet"
        ],
        "summary": "Administer a fleet runner",
        "description": "Platform-admin mutation on a single runner. The body carries exactly one of `action` or `assigned_policy`. `action` moves the admin state: `cordon` to cordoned, `drain` to draining, `revoke` to revoked. Revoked is terminal; a revoked runner cannot transition back. `self_test` moves no state. It records a request for the runner to test its own sandbox. The host picks the request up on its next heartbeat and answers on a later one. The reply is the recorded request, never a verdict. A revoked runner refuses it with 409 `UZ- RUN-018`, since it will never heartbeat again. `assigned_policy` re- assigns the runner's policy, and the host picks it up on its next heartbeat. Sending the same policy again changes nothing and records no event. ",
        "operationId": "patch_fleet_runner",
        "parameters": [
          {
            "name": "runner_id",
            "in": "path",
            "description": "`UUIDv7` of the runner.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RunnerAdminPatchRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunnerAdminPatchResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/fleets/runners/{runner_id}/events": {
      "get": {
        "tags": [
          "Fleet"
        ],
        "summary": "List fleet runner events",
        "description": "Platform-admin read of a single runner's append-only history, newest first over the composite (occurred_at, id) key. The retired page and page_size parameters are refused. Optionally filtered by event type and an occurred-at millisecond window. ",
        "operationId": "list_fleet_runner_events",
        "parameters": [
          {
            "name": "runner_id",
            "in": "path",
            "description": "`UUIDv7` of the runner.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "starting_after",
            "in": "query",
            "description": "An opaque cursor from a previous page's `next_cursor`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Rows per page (1-100).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "event_type",
            "in": "query",
            "description": "Filter to one runner event type, or a comma-separated set returning the union (multi-value equality). Allowed tags: runner_registered, runner_online, runner_offline, lease_acquired, lease_released, runner_cordoned, runner_draining, runner_drained, runner_revoked. An unrecognised tag or an empty value refuses the whole request.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "since",
            "in": "query",
            "description": "Lower bound on occurred_at, epoch milliseconds (inclusive).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "until",
            "in": "query",
            "description": "Upper bound on occurred_at, epoch milliseconds (inclusive). Must be >= since.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunnerEventsResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/fleets/runners/{runner_id}/leases": {
      "get": {
        "tags": [
          "Fleet"
        ],
        "summary": "List a runner's leases",
        "description": "Platform-admin read of a single runner's lease history, newest first. Each lease is joined to its Fleet event, so outcome and failure cause arrive in one round trip. Stripe-style keyset pagination — `starting_after` is a lease id from a previous page's `next_cursor`. Settled leases are retained for 30 days. The window is measured from settlement, not from acquisition. A lease acquired long ago and settled yesterday keeps its full window. A background sweep deletes them past that window. A lease still running or renewing is never swept. The per-lease activity records (`lease_acquired`, `lease_released`) are swept on the same 30-day window. Each record ages from when it occurred, not from its lease's settlement. A long run's opening record can therefore be pruned shortly before its lease row. The runner's lifecycle activity is kept at any age, so a long-lived runner's feed never empties. A lease whose runner died without reporting is marked expired once it passes the same window. It then keeps its own window, like any settled lease. The lifetime totals on `GET /v1/fleets/runners/{id}` are unaffected by any of this. They count transitions as they happen, not surviving rows. A long-lived runner's `leases_acquired` will therefore exceed what this endpoint returns. ",
        "operationId": "list_fleet_runner_leases",
        "parameters": [
          {
            "name": "runner_id",
            "in": "path",
            "description": "`UUIDv7` of the runner.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "starting_after",
            "in": "query",
            "description": "A lease id this runner holds; the page starts strictly after it. A lease id the runner does not hold is refused. So is one outside the `workspace_id` or `fleet` filters when those filters are set. The cursor names a position in the filtered stream, so it must belong to it. A cursor whose lease the retention sweep has since deleted is refused for the same reason — start again from the first page.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "workspace_id",
            "in": "query",
            "description": "Narrow the page and total to leases owned by one workspace. A malformed id is refused; an unknown one matches nothing.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet",
            "in": "query",
            "description": "Narrow the page and total to leases run for one fleet, named by its id or its exact name (case-insensitive). Combines with `workspace_id`; the two filters intersect. An empty or over-long value is refused; a value no fleet matches returns nothing.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Rows per page (1-100).",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/ingress/qstash/schedules": {
      "post": {
        "tags": [
          "Schedules"
        ],
        "summary": "Receive a scheduled fire",
        "description": "Receives a signed fire from Upstash QStash and appends a `cron` event to the target Fleet. A valid duplicate, missing, inactive, or stale fire is accepted without appending an event. ",
        "operationId": "ingest_qstash_schedule",
        "parameters": [
          {
            "name": "Upstash-Signature",
            "in": "header",
            "description": "QStash signature over the exact request body and destination URL.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Upstash-Schedule-Id",
            "in": "header",
            "description": "Schedule identifier supplied by QStash.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Upstash-Message-Id",
            "in": "header",
            "description": "QStash delivery identifier used for replay suppression.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Fired"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "429": {
            "description": "The instance is at its ceiling"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": []
      }
    },
    "/v1/ingress/{provider}": {
      "post": {
        "tags": [
          "Webhooks"
        ],
        "summary": "Receive a provider event",
        "description": "Receives signed events from a connected provider. GitHub is the supported provider. A matching event starts runs for subscribed fleets. Duplicate events do not start another run. Repair pull requests and workflow results update repair evidence without starting another run. A terminal production `deployment_status` records the deployed commit and schedules eligible verification fleets. ",
        "operationId": "ingest_connector_webhook",
        "parameters": [
          {
            "name": "provider",
            "in": "path",
            "description": "Provider identifier, such as `slack` or `github`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "X-GitHub-Event",
            "in": "header",
            "description": "GitHub event type, currently `ping`, `pull_request`, `workflow_run`, or `deployment_status`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "X-GitHub-Delivery",
            "in": "header",
            "description": "Delivery identifier supplied by GitHub.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "X-Hub-Signature-256",
            "in": "header",
            "description": "Hash-based Message Authentication Code (HMAC)-SHA256 of the raw body, prefixed with `sha256=`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Pong"
                }
              }
            }
          },
          "202": {
            "description": "Accepted for publication; a subscriber reading it is a separate event",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Pong"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "413": {
            "description": "The payload is over this route's ceiling"
          },
          "429": {
            "description": "The instance is at its ceiling"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": []
      }
    },
    "/v1/models": {
      "get": {
        "tags": [
          "Model Library"
        ],
        "summary": "List available models",
        "description": "Returns models available to the tenant. Each item includes its provider, context limit, and usage fields. An empty library returns an empty array. A bounded keyset page ordered by normalized `model_id`, then normalized `provider`, then an opaque row identity. Every comparison runs under `COLLATE \"C\"`, so the order is byte-wise and stable across locales. Follow `next_cursor` to read the whole catalogue; a client that reads only the first page silently loses every model past it. The response is conditionally revalidated. Every answer carries an `ETag` over the exact bytes served, plus `Cache-Control: private, no-cache` and `Vary: Authorization`. Send the tag back as `If-None-Match` and a match answers `304` with no body and the same headers. `private` is what stops a shared proxy handing one tenant's response to another even though the payload is identical for every authorized caller. ",
        "operationId": "get_model_library",
        "parameters": [
          {
            "name": "provider",
            "in": "query",
            "description": "Exact match on the normalized provider. At most 128 bytes once\nnormalized; empty after normalization is treated as absent.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "starting_after",
            "in": "query",
            "description": "Opaque cursor from a previous page's `next_cursor`, bound to the filters\nand page size that produced it. Only the sort boundary is read from it —\nthe filters applied are always the request's.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Rows per page, 1..100. Defaults to 50.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "304": {
            "description": "Not modified — the entity tag the caller presented still holds"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/runners": {
      "post": {
        "tags": [
          "Fleet"
        ],
        "summary": "Register a runner",
        "description": "Enrolls a runner into the fleet and assigns its policy. Requires an existing operator credential (Clerk JWT or `agt_t` API key with admin role); there is no enrollment token. Mints a durable `agt_r` runner token, returned once, and stores only its SHA-256 hash. The host applies the policy assigned here and never declares its own. ",
        "operationId": "register_runner",
        "responses": {
          "201": {
            "description": "Runner registered; the bearer token is returned exactly once",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/runners/me": {
      "get": {
        "tags": [
          "Runners"
        ],
        "summary": "Read this runner's own row",
        "description": "Answers the row this host enrolled as: its status, its assigned policy, and what its kernel can actually enforce. Reading it does NOT bump liveness — the heartbeat writes that and nothing else does, so inspecting a host can never mask a dead runner. ",
        "operationId": "get_runner_self",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SelfResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/runners/me/bundles/{content_hash}": {
      "get": {
        "tags": [
          "Fleet Bundles"
        ],
        "summary": "Fetch a fleet's support-file bundle",
        "description": "The canonical tar a runner materialises support files from, addressed by content hash. The daemon proxies it because the runner holds no object-store keys. A hash is not a name anybody can guess, so the snapshot is not scoped to a runner, a fleet or a tenant. ",
        "operationId": "runner_fetch_bundle",
        "parameters": [
          {
            "name": "content_hash",
            "in": "path",
            "description": "Content hash of the bundle.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/runners/me/credentials/mint": {
      "post": {
        "tags": [
          "Runners"
        ],
        "summary": "Mint a credential for a declared tool",
        "description": "Issued at the moment a tool needs one, scoped to what the lease already proved. The body names no workspace. The credential is minted against the lease the caller holds, so a runner cannot ask for material outside its own run. ",
        "operationId": "runner_mint_credential",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MintCredentialRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MintCredentialResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/runners/me/heartbeats": {
      "post": {
        "tags": [
          "Runners"
        ],
        "summary": "Report liveness, receive assignment",
        "description": "Liveness up, configuration down. The assignment rides every reply. An operator's dashboard change therefore reaches the host within one interval, and nobody visits the host. An unreadable body is read as `no report this beat` and the beat still counts. A host must not be able to fail its own liveness by sending nonsense. ",
        "operationId": "runner_heartbeat",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HeartbeatResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/runners/me/leases": {
      "post": {
        "tags": [
          "Runners"
        ],
        "summary": "Poll for the next lease",
        "description": "The poll a runner lives on. Answers either a lease to run or a backoff to wait out. The claim, the money, the gates and the policy are all decided before the answer is written. ",
        "operationId": "runner_lease",
        "responses": {
          "200": {
            "description": "OK"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/runners/me/leases/{lease_id}/activity": {
      "post": {
        "tags": [
          "Runners"
        ],
        "summary": "Publish live-tail activity frames",
        "description": "Frames for the workspace event stream. Answers 202 rather than 200: the frame is accepted for publication, and a subscriber reading it is a separate event from this call returning. ",
        "operationId": "runner_publish_activity",
        "parameters": [
          {
            "name": "lease_id",
            "in": "path",
            "description": "`UUIDv7` of the lease.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ActivityRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Accepted for publication; a subscriber reading it is a separate event"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/runners/me/leases/{lease_id}/renew": {
      "post": {
        "tags": [
          "Runners"
        ],
        "summary": "Extend a live lease",
        "description": "A run buying more time before its lease expires. The body is optional. A body that will not parse is refused rather than read as an empty one. A renewal asserts progress, and an unreadable assertion is not one. ",
        "operationId": "runner_renew_lease",
        "parameters": [
          {
            "name": "lease_id",
            "in": "path",
            "description": "`UUIDv7` of the lease.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RenewResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/runners/me/memory/{fleet_id}": {
      "get": {
        "tags": [
          "Memory"
        ],
        "summary": "Load what a fleet remembers",
        "description": "The memory a fleet carries between runs, read at the start of one. The runner names the fleet, and the lease it holds is what makes that name legitimate. ",
        "operationId": "runner_hydrate_memory",
        "parameters": [
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemoryHydrateResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "post": {
        "tags": [
          "Memory"
        ],
        "summary": "Capture what a fleet learned",
        "description": "Writes back what the run decided is worth keeping. The reply names what was stored and what was skipped, so a runner learns which of its entries did not survive the bounds. ",
        "operationId": "runner_capture_memory",
        "parameters": [
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MemoryPushRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemoryCaptureResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/runners/me/reports": {
      "post": {
        "tags": [
          "Runners"
        ],
        "summary": "Report the result of one run",
        "description": "The terminal result of one lease. A report from a holder the fleet has already superseded is refused by the fence and writes nothing. A stale writer therefore cannot land a partial finalize on the current holder's run. ",
        "operationId": "runner_report",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReportRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReportResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/tenants/me/billing": {
      "get": {
        "tags": [
          "Billing"
        ],
        "summary": "Read the tenant balance",
        "description": "Returns the balance shared by every workspace in the tenant. The response also shows whether the balance is empty. ",
        "operationId": "get_tenant_billing",
        "parameters": [
          {
            "name": "cursor",
            "in": "query",
            "description": "Token from `next_cursor` in the previous response. Omit it for the first page.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Number of records to return. The default is 50. Valid values are 1 through 200.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BillingResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/tenants/me/billing/charges": {
      "get": {
        "tags": [
          "Billing"
        ],
        "summary": "List tenant charges",
        "description": "Returns charge records with the newest record first. Use `limit` and `cursor` to read more records. ",
        "operationId": "get_tenant_billing_charges",
        "parameters": [
          {
            "name": "cursor",
            "in": "query",
            "description": "Token from `next_cursor` in the previous response. Omit it for the first page.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Number of records to return. The default is 50. Valid values are 1 through 200.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChargesResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/tenants/me/models": {
      "get": {
        "tags": [
          "Tenant"
        ],
        "summary": "List the tenant's model registry",
        "description": "One entry per configured model; two entries can share a `secret_ref`. Each entry is joined to its secret's non-secret metadata (provider, kind, base_url, has_key) — `api_key` is never serialised. `active` is computed against the tenant's current provider selection (`GET /v1/tenants/me/provider`). The response is a bounded page ordered by `created_at` descending, then `id` descending. Page forward by sending the response's `next_cursor` back as `starting_after`; a `null` `next_cursor` is the last page. Cursors are bound to the tenant and the `limit` that produced them, so changing `limit` mid-pagination requires starting from the first page again. ",
        "operationId": "list_tenant_model_entries",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ModelEntriesResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "post": {
        "tags": [
          "Tenant"
        ],
        "summary": "Register a model entry",
        "description": "Creates one `(model_id, secret_ref)` row. `secret_ref` must already name a vault secret in the tenant's primary workspace — store or reuse one via `/v1/workspaces/{workspace_id}/secrets` first. Does not activate the entry; activate via `PUT /v1/tenants/me/provider`. ",
        "operationId": "create_tenant_model_entry",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateModelEntryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StoredModelEntry"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/tenants/me/models/{id}": {
      "delete": {
        "tags": [
          "Tenant"
        ],
        "summary": "Remove a model entry",
        "description": "Idempotent — deleting an id that doesn't exist (already removed, or never existed) still returns 204. The referenced vault secret is never touched; sibling entries sharing it survive. ",
        "operationId": "delete_tenant_model_entry",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "`UUIDv7` of the resource.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "patch": {
        "tags": [
          "Tenant"
        ],
        "summary": "Change a model entry's model_id",
        "description": "Model-only change; `secret_ref` is immutable on this endpoint — create a new entry to point at a different secret. ",
        "operationId": "update_tenant_model_entry",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "`UUIDv7` of the resource.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateModelEntryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StoredModelEntry"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/tenants/me/provider": {
      "get": {
        "tags": [
          "Tenant"
        ],
        "summary": "Read tenant model provider settings",
        "description": "Returns the tenant's provider mode, provider name, model, context limit, and credential reference. Secret values are never returned. ",
        "operationId": "get_tenant_provider",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TenantProviderResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "put": {
        "tags": [
          "Tenant"
        ],
        "summary": "Set tenant model provider",
        "description": "Sets the provider mode and model. Self-managed mode requires a `credential_ref` that names an existing secret. If the model entry does not exist, this request creates it. ",
        "operationId": "put_tenant_provider",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TenantProviderRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TenantProviderResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "delete": {
        "tags": [
          "Tenant"
        ],
        "summary": "Reset tenant LLM provider to platform default",
        "description": "Equivalent to `PUT {mode: \"platform\"}`. Writes the explicit mode=platform row so the dashboard can distinguish \"never configured\" from \"explicitly reset\". ",
        "operationId": "delete_tenant_provider",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TenantProviderResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/tenants/me/workspaces": {
      "get": {
        "tags": [
          "Workspaces"
        ],
        "summary": "List the tenant's workspaces",
        "description": "Returns a stable oldest-first cursor page of workspaces owned by the caller's authoritative tenant. Pass `starting_after` from `next_cursor` to continue. The optional `name` filter uses exact equality and supports reconciliation after an uncertain workspace- create response. ",
        "operationId": "list_tenant_workspaces",
        "parameters": [
          {
            "name": "name",
            "in": "query",
            "description": "Exact tenant-scoped workspace name.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "starting_after",
            "in": "query",
            "description": "Cursor returned as `next_cursor` by the previous page.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "How many rows to return.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspacesResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/webhooks/svix/{fleet_id}": {
      "post": {
        "tags": [
          "Webhooks"
        ],
        "summary": "Receive a signed Svix event",
        "description": "Receives a signed Svix event for one fleet. A valid new event returns 202. A duplicate or an event for a paused fleet returns 200. ",
        "operationId": "receive_svix_webhook",
        "parameters": [
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "svix-id",
            "in": "header",
            "description": "Svix message identifier (used for deduplication and signature binding).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "svix-timestamp",
            "in": "header",
            "description": "Unix timestamp of the Svix delivery (used for replay protection).",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "svix-signature",
            "in": "header",
            "description": "Space-separated list of Svix signatures (v1 scheme).",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "202": {
            "description": "Accepted for publication; a subscriber reading it is a separate event"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/webhooks/{fleet_id}": {
      "post": {
        "tags": [
          "Fleets"
        ],
        "summary": "Send a signed event to a fleet",
        "description": "Sends a signed event to one fleet. Every request requires a valid provider signature. A new event returns 202. A duplicate within 24 hours returns `status: duplicate`. A paused fleet returns `ignored: fleet_paused`. Send the event again after resuming the fleet. ",
        "operationId": "receive_webhook",
        "parameters": [
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "202": {
            "description": "Accepted for publication; a subscriber reading it is a separate event"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/webhooks/{fleet_id}/approval": {
      "post": {
        "tags": [
          "Webhooks"
        ],
        "summary": "Resolve a fleet approval gate",
        "description": "Called by a human (via email link or Slack action) to approve or reject a paused fleet. Body is HMAC-signed by the issuer; the signature is verified against the fleet's webhook secret. ",
        "operationId": "approval_webhook",
        "parameters": [
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Resolved"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/webhooks/{fleet_id}/github": {
      "post": {
        "tags": [
          "Webhooks"
        ],
        "summary": "Receive a signed GitHub event",
        "description": "Verifies the body with the `webhook_secret` field from the workspace's `github` secret. Reviewable `pull_request` events and failed completed `workflow_run` events create fleet events. A delivery identifier remains reserved for 72 hours after acceptance. ",
        "operationId": "github_webhook",
        "parameters": [
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "X-GitHub-Event",
            "in": "header",
            "description": "GitHub event type.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "X-GitHub-Delivery",
            "in": "header",
            "description": "GitHub-issued delivery UUID. Used as the dedupe key.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "X-Hub-Signature-256",
            "in": "header",
            "description": "HMAC-SHA256 of the raw body, prefixed with `sha256=`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "202": {
            "description": "Accepted for publication; a subscriber reading it is a separate event"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "413": {
            "description": "The payload is over this route's ceiling"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces": {
      "post": {
        "tags": [
          "Workspaces"
        ],
        "summary": "Create a workspace",
        "description": "Creates a named workspace in the caller's tenant. The server assigns the workspace identifier. This operation does not accept a replay key or retry automatically. After an uncertain response, query the tenant's workspaces with the exact name. Retrying the same tenant- unique name cannot create a second row and returns 409 when the first request committed. ",
        "operationId": "create_workspace",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/CreateWorkspaceRequest"
                  }
                ]
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CreatedWorkspaceResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/approvals": {
      "get": {
        "tags": [
          "Approvals"
        ],
        "summary": "List pending approval gates for a workspace",
        "description": "Returns approval gates oldest-first (oldest is most urgent). Each row surfaces the fleet's proposed action, gathered evidence, blast- radius assessment, and timeout countdown. Filter by fleet, gate kind, or status. Cursor pagination over (created_at, id) so concurrent inserts don't cause silent skips. ",
        "operationId": "list_workspace_approvals",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "status",
            "in": "query",
            "description": "Defaults to \"pending\". Supply \"approved\", \"denied\", \"timed_out\" to query terminal states.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "gate_kind",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApprovalsResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/approvals/{gate_id}": {
      "get": {
        "tags": [
          "Approvals"
        ],
        "summary": "Get a single approval gate by id",
        "description": "Drives the dashboard detail page. 404 when the gate doesn't exist or belongs to a different workspace. ",
        "operationId": "get_workspace_approval",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "gate_id",
            "in": "path",
            "description": "`UUIDv7` of the approval gate.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApprovalSummary"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/approvals/{gate_id}/{decision}": {
      "post": {
        "tags": [
          "Approvals"
        ],
        "summary": "Approve a pending request",
        "description": "Approves a pending request. If two callers resolve it, the first result wins. Other callers receive 409 with the existing result and resolver. ",
        "operationId": "approve_workspace_approval",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "gate_id",
            "in": "path",
            "description": "`UUIDv7` of the approval gate.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "decision",
            "in": "path",
            "description": "What to do with the gate: `approve` or `deny`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResolvedResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/connectors": {
      "get": {
        "tags": [
          "Connectors"
        ],
        "summary": "List connectors for a workspace",
        "description": "Returns the providers available to the workspace. Each item shows whether the provider is ready and connected. Requires the `connector:read` scope. ",
        "operationId": "connector_catalog",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/CatalogueEntry"
                  }
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/connectors/{provider}": {
      "get": {
        "tags": [
          "Connectors"
        ],
        "summary": "Read a connector status",
        "description": "Returns `connected` or `not_connected` for one provider. Requires the `connector:read` scope. An unknown provider returns 404 `UZ- CONN-004`. ",
        "operationId": "connector_status",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "provider",
            "in": "path",
            "description": "Provider identifier, such as `slack` or `github`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConnectionView"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "delete": {
        "tags": [
          "Connectors"
        ],
        "summary": "Disconnect a provider from a workspace",
        "description": "Removes the workspace's connector handle and event-routing records from `agentsfleet`. Provider authorization remains active outside `agentsfleet`. Repeating this request returns 204. Use Connect to authorize the provider again. ",
        "operationId": "connector_disconnect",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "provider",
            "in": "path",
            "description": "Provider identifier, such as `slack` or `github`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/connectors/{provider}/connect": {
      "post": {
        "tags": [
          "Connectors"
        ],
        "summary": "Get a provider connection URL",
        "description": "Returns a URL for connecting the workspace to the provider. Open the URL in a browser. An unknown provider returns 404 `UZ-CONN-004`. An unavailable provider returns 503 `UZ-CONN-001`. ",
        "operationId": "connector_connect",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "provider",
            "in": "path",
            "description": "Provider identifier, such as `slack` or `github`.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConsentRedirect"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/events": {
      "get": {
        "tags": [
          "Workspaces"
        ],
        "summary": "List a workspace's aggregate event history",
        "description": "Cursor-paginated list of operator-visible events across every fleet in the workspace, newest first. Replaces the deleted activity endpoint. Optional `fleet_id` filter narrows to one fleet. Same query shape as the per-fleet endpoint - `actor` glob, `since=` (duration or RFC 3339), and a `cursor` mutually exclusive with `since`. ",
        "operationId": "list_workspace_events",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "query",
            "description": "Drill down to one fleet within the workspace.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque base64url-no-pad cursor returned in `next_cursor`. Mutually exclusive with `since`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "actor",
            "in": "query",
            "description": "Glob filter against the row's actor. `*` is a wildcard.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "since",
            "in": "query",
            "description": "Lower bound on `created_at`. Accepts Go-style durations (15s/30m/2h/7d) or RFC 3339 timestamps.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "How many rows to return.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EventsResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/events/stream": {
      "get": {
        "tags": [
          "Workspaces"
        ],
        "summary": "Stream live activity for a whole workspace",
        "description": "Opens ONE Server-Sent Events (SSE) stream carrying live activity for every fleet the caller can read in the workspace. It is the Fleets Wall's single connection, replacing one stream per tile. The first frame is `event: hello`. Its `data` is `{\"kind\":\"hello\",\"fleet_ids\":[...]}` for the current readable fleet set. Activity frame `data` contains the publisher payload with `fleet_id` added as the leading field. The client routes each frame by that tag. A stalled client can overflow its bounded server queue. The server then sends `event: catching_up` with `{\"kind\":\"catching_up\",\"dropped\":N}`. `dropped` is the new drop count since the previous signal. Control frames use identifier 0 and do not advance the activity sequence. Activity identifiers start at 0 for each connection. The route ignores `Last-Event-ID`. The connection adjusts its fan-in as fleets appear or disappear. A caller whose workspace access is revoked stops receiving on the next refresh. At capacity the route returns 503 `UZ-API-002` with `Retry- After`. After a reconnect opens, recover the gap through `GET /v1/workspaces/{workspace_id}/events`. ",
        "operationId": "stream_workspace_events",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleet-libraries": {
      "get": {
        "tags": [
          "Fleet library"
        ],
        "summary": "List the workspace Fleet library gallery",
        "description": "Returns the union of the platform Fleet library catalog and this workspace's own tenant entries — and nothing from another workspace. Each entry carries identity, source, requirements and per-credential reason copy; never an object-store key. A bounded keyset page ordered by `created_at DESC, tier ASC, id DESC` across BOTH catalogs. Follow `next_cursor` to read the whole gallery; a client that reads only the first page silently loses every entry past it. The support-file manifest is stored on import but served by no endpoint. ",
        "operationId": "list_workspace_fleet_library",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Rows per page, 1..100. Defaults to 50.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "starting_after",
            "in": "query",
            "description": "Opaque cursor from a previous page's `next_cursor`. Bound to the workspace and page size that produced it; a mismatch is `UZ-LIBRARY-002` rather than a silently different page.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GalleryResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "post": {
        "tags": [
          "Fleet library"
        ],
        "summary": "Onboard a tenant Fleet library entry",
        "description": "Onboards a Fleet library entry into the caller's workspace catalog from a GitHub source reference. Requires the `library:write` scope plus ownership of the target workspace. Re-onboarding identical bytes converges on one `(workspace_id, content_hash)` row. The response carries metadata only. ",
        "operationId": "onboard_tenant_fleet_library",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminLibraryCreated"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "413": {
            "description": "The payload is over this route's ceiling"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets": {
      "get": {
        "tags": [
          "Fleets"
        ],
        "summary": "List fleets in a workspace",
        "description": "Returns visible fleets with the newest fleet first. Omit `starting_after` for the first page. Use the returned `next_cursor` to read the next page. ",
        "operationId": "list_fleets",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "starting_after",
            "in": "query",
            "description": "Opaque page token from a prior response's `next_cursor` field, formatted `{created_at_epoch_ms}:{fleet_id}`. Omit for the first page. The retired `cursor` spelling is refused.\n",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Page size. Defaults to 20. Clamped to a maximum of 100.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FleetsResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "post": {
        "tags": [
          "Fleets"
        ],
        "summary": "Create a fleet",
        "description": "Creates a fleet from one library entry. Use either `platform_library_id` or `tenant_library_id`. The service creates a default API trigger when the library has no trigger. ",
        "operationId": "create_fleet",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InstallFleetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstalledFleetResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "424": {
            "description": "A dependency of this request failed"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}": {
      "get": {
        "tags": [
          "Fleets"
        ],
        "summary": "Get a fleet",
        "description": "Returns one fleet's editable source, trigger markdown, bundle pin, trigger list, status, and lifetime counters. The response carries an `ETag` header over `source_markdown` and `trigger_markdown`. Send that value as `If-Match` when saving source changes to avoid overwriting another operator's edit. ",
        "operationId": "get_fleet",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FleetDetailResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "delete": {
        "tags": [
          "Fleets"
        ],
        "summary": "Permanently delete a fleet",
        "description": "Permanently deletes the fleet, events, runs, approvals, grants, keys, and memory. This cannot be undone. Set the fleet status to `killed` first. Otherwise the request returns 409. Requires an operator role. ",
        "operationId": "delete_fleet",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "patch": {
        "tags": [
          "Fleets"
        ],
        "summary": "Update a fleet",
        "description": "Updates fleet files or status. Every request field is optional. `config_json` and `trigger_markdown` cannot be used together. A killed fleet cannot be changed. Source editors can send `If-Match` with the `ETag` from GET. A stale tag returns 412 with the current tag in the problem body; omitting the header preserves last-write- wins behavior. Status changes require an operator role. Config changes require workspace membership. ",
        "operationId": "patch_fleet",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "If-Match",
            "in": "header",
            "description": "Optional source-version tag from GET. Stale values return 412 with the current `etag`.",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PatchedFleetResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "412": {
            "description": "The precondition the caller supplied does not hold"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/events": {
      "get": {
        "tags": [
          "Fleets"
        ],
        "summary": "List a fleet's event history",
        "description": "Returns fleet events with the newest event first. Filter with `actor` or `since`. You cannot use `since` and `cursor` together. ",
        "operationId": "list_fleet_events",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "description": "Opaque base64url-no-pad cursor returned in `next_cursor`. Mutually exclusive with `since`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "actor",
            "in": "query",
            "description": "Glob filter against the row's actor. `*` is a wildcard.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "since",
            "in": "query",
            "description": "Lower bound on `created_at`. Accepts Go-style durations (15s/30m/2h/7d) or RFC 3339 timestamps.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "How many rows to return.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EventsResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/events/stream": {
      "get": {
        "tags": [
          "Fleets"
        ],
        "summary": "Stream live fleet activity",
        "description": "Opens a Server-Sent Events (SSE) stream for new fleet activity. Each message includes `id`, `event`, and `data`. Identifiers restart at 0 for each connection. The route ignores `Last-Event-ID`. At capacity, the route returns 503 `UZ-API-002` with `Retry-After`. Read missed events before reconnecting. ",
        "operationId": "stream_fleet_events",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/events/{event_id}": {
      "get": {
        "tags": [
          "Fleets"
        ],
        "summary": "Read one fleet event",
        "description": "Returns a single event with its request body and the agent's full answer. The list read beside this one omits both bodies: it returns up to two hundred rows and would carry every payload to render a table. Read the body here, for the one event you are expanding. An event that does not exist and an event belonging to another workspace both answer 404. The two are deliberately indistinguishable. The read is scoped inside its own query, so no caller can probe for events outside its own workspace. ",
        "operationId": "get_fleet_event",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "event_id",
            "in": "path",
            "description": "`UUIDv7` of the event.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EventDetail"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/integration-grants": {
      "get": {
        "tags": [
          "Integration Grants"
        ],
        "summary": "List integration grants for a fleet",
        "description": "Returns pending, approved, and revoked grants, newest first. ",
        "operationId": "list_integration_grants",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GrantsResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/integration-grants/{grant_id}": {
      "delete": {
        "tags": [
          "Integration Grants"
        ],
        "summary": "Revoke an integration grant",
        "description": "Immediately revokes a grant. A revoked grant blocks the fleet on its next call against the affected service. ",
        "operationId": "revoke_integration_grant",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "grant_id",
            "in": "path",
            "description": "`UUIDv7` of the grant.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/memories": {
      "get": {
        "tags": [
          "Memory"
        ],
        "summary": "List or search memory entries for a fleet",
        "description": "Returns memory entries for the fleet, newest-created first. Without `query` this lists entries (optionally filtered by `category`). With `query`, performs case-insensitive LIKE search across both `key` and `content` (LIKE metacharacters in `query` are escaped — a query of `%` does NOT return every entry). Every shape pages by keyset: pass the previous response's `next_cursor` as `starting_after` to continue the walk. A null `next_cursor` marks the final page. ",
        "operationId": "list_fleet_memories",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemoriesResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/memories/{key}": {
      "delete": {
        "tags": [
          "Memory"
        ],
        "summary": "Forget a fleet memory entry",
        "description": "Deletes one memory entry for this fleet. The key is scoped to the fleet, so a key from another fleet is not deleted. Missing keys return 404 so an operator can tell a mistyped key from a successful forget. ",
        "operationId": "delete_fleet_memory",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "key",
            "in": "path",
            "description": "Memory entry key.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/messages": {
      "get": {
        "tags": [
          "Fleets"
        ],
        "summary": "List a fleet's chat thread with bodies",
        "description": "Returns the newest chat events first. Each item carries the trigger payload (`request_json`) and the agent's full answer (`response_text`). One request replaces reading the event list and then each event's detail. A page holds at most `limit` items and at most 512 KiB of encoded items. The newest item always ships, even alone. Follow `next_cursor` to read the rest. ",
        "operationId": "list_fleet_messages",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "starting_after",
            "in": "query",
            "description": "Opaque continuation cursor from a previous page's `next_cursor`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ThreadResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "post": {
        "tags": [
          "Fleets"
        ],
        "summary": "Post a chat message to a fleet",
        "description": "Starts a fleet run with a chat event. Returns an event identifier for tracking in the activity stream. ",
        "operationId": "post_fleet_message",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "202": {
            "description": "Accepted for publication; a subscriber reading it is a separate event",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SteerAccepted"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/schedules": {
      "get": {
        "tags": [
          "Schedules"
        ],
        "summary": "List hosted schedules",
        "description": "Lists the hosted schedules for a Fleet. The result is bounded by the per-Fleet schedule cap. ",
        "operationId": "list_fleet_schedules",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Page"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "post": {
        "tags": [
          "Schedules"
        ],
        "summary": "Create a hosted schedule",
        "description": "Creates a schedule and synchronously registers it in Upstash QStash. ",
        "operationId": "create_fleet_schedule",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "201": {
            "description": "Created"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "422": {
            "description": "The request was read and its content cannot be acted on"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/schedules/{schedule_id}": {
      "get": {
        "tags": [
          "Schedules"
        ],
        "summary": "Get a hosted schedule",
        "description": "One schedule, as it is currently registered for this fleet.",
        "operationId": "get_fleet_schedule",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "schedule_id",
            "in": "path",
            "description": "`UUIDv7` of the schedule.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/View"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "delete": {
        "tags": [
          "Schedules"
        ],
        "summary": "Delete a hosted schedule",
        "description": "Deletes the QStash registration before removing the local schedule row. ",
        "operationId": "delete_fleet_schedule",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "schedule_id",
            "in": "path",
            "description": "`UUIDv7` of the schedule.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "patch": {
        "tags": [
          "Schedules"
        ],
        "summary": "Update a hosted schedule",
        "description": "Updates schedule fields and synchronously overwrites the QStash registration. ",
        "operationId": "update_fleet_schedule",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "schedule_id",
            "in": "path",
            "description": "`UUIDv7` of the schedule.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "422": {
            "description": "The request was read and its content cannot be acted on"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/fleets/{fleet_id}/schedules/{schedule_id}/sync": {
      "post": {
        "tags": [
          "Schedules"
        ],
        "summary": "Sync a hosted schedule",
        "description": "Idempotently overwrites the QStash registration from the latest local generation. ",
        "operationId": "sync_fleet_schedule",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "fleet_id",
            "in": "path",
            "description": "`UUIDv7` of the fleet.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "schedule_id",
            "in": "path",
            "description": "`UUIDv7` of the schedule.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/onboarding": {
      "get": {
        "tags": [
          "Workspaces"
        ],
        "summary": "Read the workspace's onboarding checklist state",
        "description": "Returns every signal the Getting Started checklist needs in one call. Five signals are derived server-side in a single query. A model is configured, a fleet exists, a credential exists, an event has been processed, and a steer event exists. Three more are the caller's stored UI preferences: dismissed, collapsed, and CLI ticked. This replaces six separate requests with one. ",
        "operationId": "get_workspace_onboarding",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OnboardingResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/preferences": {
      "get": {
        "tags": [
          "Workspaces"
        ],
        "summary": "Read the caller's dashboard preferences for a workspace",
        "description": "Returns every dashboard preference the calling user has set in this workspace, as an object keyed by preference key. A user who has set nothing gets `{\"prefs\": {}}` - never a 404, so a client can tell \"no preferences\" from \"preferences unavailable\" without branching. Preferences are per user AND per workspace: a second workspace starts its onboarding checklist fresh. ",
        "operationId": "get_workspace_preferences",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PreferencesResponse"
                }
              }
            }
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/preferences/{pref_key}": {
      "put": {
        "tags": [
          "Workspaces"
        ],
        "summary": "Write one dashboard preference",
        "description": "Writes a single preference for the calling user in this workspace. The response carries the full updated set. The request body is the value itself: any well-formed JavaScript Object Notation (JSON) value up to 1 KiB. agentsfleet stores it as sent and never reads inside it. `pref_key` must be one the dashboard declares. agentsfleet refuses anything else with `UZ-PREFS-001` and writes no row. Concurrent writes to one key are last-write-wins by design: a preference is a single toggle, so a lost write costs one click. ",
        "operationId": "put_workspace_preference",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "pref_key",
            "in": "path",
            "description": "The preference key.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/secrets": {
      "get": {
        "tags": [
          "Secrets"
        ],
        "summary": "List secrets stored for a workspace",
        "description": "Returns secret names and non-secret details. Secret values are never returned. ",
        "operationId": "list_workspace_secrets",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SecretsResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "post": {
        "tags": [
          "Secrets"
        ],
        "summary": "Store a workspace secret",
        "description": "Stores an encrypted JSON object. Secret values are never returned. `data` must be a non-empty object no larger than 4 KiB. Larger values return `UZ-VAULT-002`. Strings, arrays, scalars, and empty objects return `UZ-VAULT-001`. Creating claims a name that is free. A name already held in this workspace returns `UZ-VAULT-005` and nothing is written. Replace the existing secret's whole body with `PUT` on the item route instead. The database decides the winner, so two concurrent creates on one name resolve to one `201` and one `409`. ",
        "operationId": "store_workspace_secret",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StoreSecretRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StoredSecretResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "409": {
            "description": "The request conflicts with the resource's current state"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    },
    "/v1/workspaces/{workspace_id}/secrets/{name}": {
      "put": {
        "tags": [
          "Secrets"
        ],
        "summary": "Replace a secret's stored body",
        "description": "Replaces the stored object in full. Send the secret you want stored, in the same `data` shape `create` takes — a field you omit is absent from the secret afterwards. Replacement is total by design. A stored secret is never readable, so a partial write cannot be reasoned about by the caller. Every field needed to rebuild the body is already returned by `GET /v1/workspaces/{workspace_id}/secrets`. The secret itself is the one exception, and this call supplies it. The name must already be held. A name this workspace does not have returns `UZ-VAULT-003` and nothing is created — claiming a name is `create`'s job. ",
        "operationId": "replace_workspace_secret",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "description": "The name the secret is stored under.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReplaceSecretRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StoredSecretResponse"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      },
      "delete": {
        "tags": [
          "Secrets"
        ],
        "summary": "Delete a secret from the workspace vault",
        "description": "Idempotent — returns 204 whether or not the secret existed. ",
        "operationId": "delete_workspace_secret",
        "parameters": [
          {
            "name": "workspace_id",
            "in": "path",
            "description": "`UUIDv7` of the workspace.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "description": "The name the secret is stored under.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "No content"
          },
          "400": {
            "description": "The request could not be read"
          },
          "401": {
            "description": "No credential, or one this route does not accept"
          },
          "403": {
            "description": "The credential is good and lacks the capability this route requires"
          },
          "404": {
            "description": "No such resource, or none this caller may see"
          },
          "500": {
            "description": "The daemon failed to answer"
          }
        },
        "security": [
          {
            "BearerAuth": []
          }
        ]
      }
    }
  },
  "components": {
    "schemas": {
      "ActivityFrame": {
        "oneOf": [
          {
            "type": "object",
            "description": "A tool call began.",
            "required": [
              "tool_call_started"
            ],
            "properties": {
              "tool_call_started": {
                "$ref": "#/components/schemas/ToolCallStarted",
                "description": "A tool call began."
              }
            }
          },
          {
            "type": "object",
            "description": "The fleet produced output.",
            "required": [
              "fleet_response_chunk"
            ],
            "properties": {
              "fleet_response_chunk": {
                "$ref": "#/components/schemas/FleetResponseChunk",
                "description": "The fleet produced output."
              }
            }
          },
          {
            "type": "object",
            "description": "A tool call finished.",
            "required": [
              "tool_call_completed"
            ],
            "properties": {
              "tool_call_completed": {
                "$ref": "#/components/schemas/ToolCallCompleted",
                "description": "A tool call finished."
              }
            }
          },
          {
            "type": "object",
            "description": "A long-running tool is still working.",
            "required": [
              "tool_call_progress"
            ],
            "properties": {
              "tool_call_progress": {
                "$ref": "#/components/schemas/ToolCallProgress",
                "description": "A long-running tool is still working."
              }
            }
          }
        ],
        "description": "One progress frame."
      },
      "ActivityRequest": {
        "type": "object",
        "description": "`POST /v1/runners/me/leases/{lease_id}/activity` request — a batch of frames.\n\nOne frame per request today; the array shape lets a later change coalesce\nwithout a wire change. The reply is `202` with no acknowledgement.",
        "required": [
          "frames"
        ],
        "properties": {
          "frames": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ActivityFrame"
            },
            "description": "The frames being forwarded."
          }
        },
        "additionalProperties": false
      },
      "AdminLibrariesResponse": {
        "type": "object",
        "description": "Admin Fleet-library list response.",
        "required": [
          "entries"
        ],
        "properties": {
          "entries": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AdminLibraryItem"
            },
            "description": "Every draft and public row."
          }
        },
        "additionalProperties": false
      },
      "AdminLibraryCreated": {
        "type": "object",
        "description": "Successful platform Fleet-library onboarding.",
        "required": [
          "id",
          "name",
          "visibility",
          "content_hash",
          "requirements"
        ],
        "properties": {
          "content_hash": {
            "type": "string",
            "description": "Content identity of the validated bundle."
          },
          "id": {
            "type": "string",
            "description": "Slug derived from `SKILL.md`."
          },
          "name": {
            "type": "string",
            "description": "Display name derived from `SKILL.md`."
          },
          "requirements": {
            "$ref": "#/components/schemas/AdminLibraryRequirements",
            "description": "Credential/tool/host names without support-file paths."
          },
          "visibility": {
            "type": "string",
            "description": "Which library the entry now stands in.\n\n`platform` from the operator's catalogue and `tenant` from a workspace\nonboard — both verbs answer this shape, and the tier is what differs."
          }
        },
        "additionalProperties": false
      },
      "AdminLibraryItem": {
        "type": "object",
        "description": "One metadata-only platform Fleet-library row.",
        "required": [
          "id",
          "name",
          "description",
          "source_repo",
          "source_ref",
          "visibility",
          "requirements",
          "required_credentials_reasons",
          "updated_at",
          "etag"
        ],
        "properties": {
          "content_hash": {
            "type": [
              "string",
              "null"
            ],
            "description": "Content identity, never support-file bytes."
          },
          "description": {
            "type": "string",
            "description": "Curated description."
          },
          "etag": {
            "type": "string",
            "description": "Strong version over the editable row surface."
          },
          "id": {
            "type": "string",
            "description": "Slug identity."
          },
          "name": {
            "type": "string",
            "description": "Display name."
          },
          "required_credentials_reasons": {
            "description": "Operator-authored per-credential reason copy."
          },
          "requirements": {
            "$ref": "#/components/schemas/AdminLibraryRequirements",
            "description": "Derived requirement names and trigger presence."
          },
          "source_ref": {
            "type": "string",
            "description": "Fetched revision."
          },
          "source_repo": {
            "type": "string",
            "description": "GitHub owner/repository."
          },
          "updated_at": {
            "type": "integer",
            "format": "int64",
            "description": "Last mutation instant in epoch milliseconds."
          },
          "visibility": {
            "type": "string",
            "description": "Draft or public."
          }
        },
        "additionalProperties": false
      },
      "AdminLibraryRequirements": {
        "type": "object",
        "description": "Content-free requirements shown on one Fleet-library row.",
        "required": [
          "credentials",
          "tools",
          "network_hosts",
          "trigger_present"
        ],
        "properties": {
          "credentials": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Credential names only."
          },
          "network_hosts": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Declared outbound hosts."
          },
          "tools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Required tool names."
          },
          "trigger_present": {
            "type": "boolean",
            "description": "Whether a trigger document exists."
          }
        },
        "additionalProperties": false
      },
      "AdminModelCreated": {
        "allOf": [
          {
            "$ref": "#/components/schemas/AdminModelItem",
            "description": "The created model fields, flattened on the existing wire."
          },
          {
            "type": "object",
            "required": [
              "request_id"
            ],
            "properties": {
              "request_id": {
                "type": "string",
                "description": "Server-generated support correlation id."
              }
            }
          }
        ],
        "description": "Successful model creation."
      },
      "AdminModelItem": {
        "allOf": [
          {
            "$ref": "#/components/schemas/ModelRates",
            "description": "Rates and context cap flattened on the existing wire."
          },
          {
            "type": "object",
            "required": [
              "id",
              "provider",
              "model_id"
            ],
            "properties": {
              "id": {
                "type": "string",
                "description": "Opaque `UUIDv7` row identity."
              },
              "model_id": {
                "type": "string",
                "description": "Provider-native model identity."
              },
              "provider": {
                "type": "string",
                "description": "Provider identity."
              }
            }
          }
        ],
        "description": "One priced model row in the admin list."
      },
      "AdminModelUpdated": {
        "type": "object",
        "description": "Successful model rate update.",
        "required": [
          "id",
          "updated",
          "request_id"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Updated row identity."
          },
          "request_id": {
            "type": "string",
            "description": "Server-generated support correlation id."
          },
          "updated": {
            "type": "boolean",
            "description": "Always true after an update."
          }
        },
        "additionalProperties": false
      },
      "AdminModelsResponse": {
        "type": "object",
        "description": "`GET /v1/admin/models` response.",
        "required": [
          "models",
          "request_id"
        ],
        "properties": {
          "models": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AdminModelItem"
            },
            "description": "Every priced row."
          },
          "request_id": {
            "type": "string",
            "description": "Server-generated support correlation id."
          }
        },
        "additionalProperties": false
      },
      "AdminState": {
        "type": "string",
        "description": "Operator intent for a runner. Only `Active` admits a runner-plane call;\nevery other value rejects one.",
        "enum": [
          "active",
          "cordoned",
          "draining",
          "drained",
          "revoked"
        ]
      },
      "ApiKeySummary": {
        "type": "object",
        "description": "One key as a list shows it: metadata, never a secret.",
        "required": [
          "id",
          "key_name",
          "active",
          "created_at"
        ],
        "properties": {
          "active": {
            "type": "boolean",
            "description": "Whether it still authenticates."
          },
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was minted."
          },
          "id": {
            "type": "string",
            "description": "The key's identifier."
          },
          "key_name": {
            "type": "string",
            "description": "What it is called."
          },
          "last_used_at": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "When it last authenticated, if it ever has."
          },
          "revoked_at": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "When it stopped working, if it has.\n\nEmitted as `null` rather than omitted — here and on every optional in\nthis module. The Zig daemon serialises through `res.json(value, .{})`,\nand std.json's default emits null optionals, so a row is always the\nsame set of keys. Omission would be a shape change a dashboard's\n`\"revoked_at\" in row` check can feel."
          }
        }
      },
      "ApprovalSummary": {
        "type": "object",
        "description": "One gate as the inbox lists it.",
        "required": [
          "gate_id",
          "fleet_id",
          "fleet_name",
          "workspace_id",
          "action_id",
          "tool_name",
          "action_name",
          "gate_kind",
          "proposed_action",
          "blast_radius",
          "status",
          "detail",
          "created_at",
          "timeout_at",
          "resolved_by"
        ],
        "properties": {
          "action_id": {
            "type": "string",
            "description": "The action it gates."
          },
          "action_name": {
            "type": "string",
            "description": "The verb it wants."
          },
          "blast_radius": {
            "type": "string",
            "description": "How far the consequences reach."
          },
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was raised."
          },
          "detail": {
            "type": "string",
            "description": "The resolver's note, empty while pending."
          },
          "evidence": {
            "type": [
              "object",
              "null"
            ],
            "description": "The evidence behind the proposal, verbatim."
          },
          "fleet_id": {
            "type": "string",
            "description": "The fleet that raised it."
          },
          "fleet_name": {
            "type": "string",
            "description": "That fleet's name, joined at read time so a rename is visible."
          },
          "gate_id": {
            "type": "string",
            "description": "The gate's own row id — what a decision addresses it by."
          },
          "gate_kind": {
            "type": "string",
            "description": "Which family of gate this is."
          },
          "proposed_action": {
            "type": "string",
            "description": "What the fleet proposes, in a person's words."
          },
          "resolved_by": {
            "type": "string",
            "description": "Who resolved it, empty while pending."
          },
          "status": {
            "type": "string",
            "description": "Where the gate stands."
          },
          "timeout_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it stops waiting."
          },
          "tool_name": {
            "type": "string",
            "description": "The tool asking."
          },
          "updated_at": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "When it was resolved, or `null` while pending."
          },
          "workspace_id": {
            "type": "string",
            "description": "The workspace it belongs to."
          }
        }
      },
      "ApprovalsResponse": {
        "type": "object",
        "description": "`GET /v1/workspaces/{workspace_id}/approvals` — the queue.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ApprovalSummary"
            },
            "description": "The gates on this page, oldest first."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the next page resumes, or `null` on the last one."
          }
        }
      },
      "ApproveSessionRequest": {
        "type": "object",
        "description": "`PATCH /v1/auth/sessions/{session_id}/approve` — a person clicked Approve.",
        "required": [
          "dashboard_public_key",
          "ciphertext",
          "nonce",
          "verification_code"
        ],
        "properties": {
          "ciphertext": {
            "type": "string",
            "description": "The sealed credential. Relayed; this daemon never opens it."
          },
          "dashboard_public_key": {
            "type": "string",
            "description": "The dashboard's public key, for the client's key agreement."
          },
          "nonce": {
            "type": "string",
            "description": "The nonce it was sealed under."
          },
          "verification_code": {
            "type": "string",
            "description": "The six digits shown to the person, whose digest is what gets stored."
          }
        },
        "additionalProperties": false
      },
      "ApproveSessionResponse": {
        "type": "object",
        "description": "What approving answers with.",
        "required": [
          "request_id"
        ],
        "properties": {
          "request_id": {
            "type": "string",
            "description": "The request that recorded the approval."
          }
        }
      },
      "AssignedPolicy": {
        "type": "object",
        "description": "The isolation, egress and concurrency settings assigned to one runner.",
        "required": [
          "sandbox_tier",
          "network_policy",
          "registry_allowlist",
          "worker_count",
          "extra_binds"
        ],
        "properties": {
          "extra_binds": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExtraBind"
            },
            "description": "Extra host paths bound into every lease's sandbox."
          },
          "network_policy": {
            "$ref": "#/components/schemas/NetworkPolicy",
            "description": "Egress posture to apply."
          },
          "registry_allowlist": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Operator registry baseline merged into each lease's egress allowlist.\nEmpty means the runner substitutes its own default registry set."
          },
          "sandbox_tier": {
            "$ref": "#/components/schemas/SandboxTier",
            "description": "Isolation strength to apply."
          },
          "worker_count": {
            "type": "integer",
            "format": "int32",
            "description": "Concurrent workers the runner may start. Clamped on both sides.",
            "minimum": 0
          }
        }
      },
      "BillingResponse": {
        "type": "object",
        "description": "`GET /v1/tenants/me/billing` — the wallet snapshot.",
        "required": [
          "balance_nanos",
          "updated_at",
          "is_exhausted"
        ],
        "properties": {
          "balance_nanos": {
            "type": "integer",
            "format": "int64",
            "description": "What remains, in nanos — one thousand-millionth of a dollar."
          },
          "exhausted_at": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "When it did, or `null` while money remains — emitted either way, for\nthe module's null rule."
          },
          "is_exhausted": {
            "type": "boolean",
            "description": "Whether the balance has reached zero."
          },
          "updated_at": {
            "type": "integer",
            "format": "int64",
            "description": "When the balance last moved."
          }
        }
      },
      "BindMode": {
        "type": "string",
        "description": "Whether an operator-added bind is writable.",
        "enum": [
          "read_only",
          "read_write"
        ]
      },
      "CapabilityReport": {
        "type": "object",
        "description": "What this host can actually enforce.\n",
        "required": [
          "landlock",
          "seccomp",
          "cgroup_controllers",
          "bubblewrap",
          "egress_enforcement"
        ],
        "properties": {
          "bubblewrap": {
            "type": "boolean",
            "description": "The sandbox launcher is available."
          },
          "cgroup_controllers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Controllers present in the delegated cgroup's subtree control."
          },
          "egress_enforcement": {
            "type": "boolean",
            "description": "Kernel-enforced egress allowlisting is available."
          },
          "landlock": {
            "type": "boolean",
            "description": "Filesystem isolation is available."
          },
          "seccomp": {
            "type": "boolean",
            "description": "System-call filtering is available."
          }
        }
      },
      "CatalogueEntry": {
        "type": "object",
        "description": "One row of `GET …/connectors`.\n\nNo secret material and no field that could carry any — the whole document is\nfour facts about availability.",
        "required": [
          "id",
          "archetype",
          "display_name",
          "configured",
          "connected"
        ],
        "properties": {
          "archetype": {
            "type": "string",
            "description": "Which flow connecting it runs — see [`ARCHETYPE_OAUTH2`]."
          },
          "configured": {
            "type": "boolean",
            "description": "Whether this DEPLOYMENT has been set up to connect it."
          },
          "connected": {
            "type": "boolean",
            "description": "Whether THIS workspace holds a landed grant for it."
          },
          "display_name": {
            "type": "string",
            "description": "The name a card shows."
          },
          "id": {
            "type": "string",
            "description": "The provider's route segment, which is also its stored id."
          }
        }
      },
      "ChargeSummary": {
        "type": "object",
        "description": "One ledger row as the charges list shows it.\n\nField-for-field the Zig `TelemetryRow`, in its order — the struct is\nserialized straight to JSON there, so the row IS the wire shape.",
        "required": [
          "id",
          "tenant_id",
          "event_id",
          "charge_type",
          "posture",
          "model",
          "credit_deducted_nanos",
          "recorded_at"
        ],
        "properties": {
          "charge_type": {
            "type": "string",
            "description": "`receive` or `stage` — the two halves of one event's cost."
          },
          "credit_deducted_nanos": {
            "type": "integer",
            "format": "int64",
            "description": "What this row drained, in nanos."
          },
          "event_id": {
            "type": "string",
            "description": "The event that triggered the work."
          },
          "fleet_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The fleet it was incurred by, under the same deletion rule."
          },
          "id": {
            "type": "string",
            "description": "The ledger row's identifier."
          },
          "model": {
            "type": "string",
            "description": "The model the stage ran against."
          },
          "posture": {
            "type": "string",
            "description": "Whose model bill the tokens landed on."
          },
          "recorded_at": {
            "type": "integer",
            "format": "int64",
            "description": "When the row was written — the walk's sort key, and what its cursor\nhalf is rendered from."
          },
          "tenant_id": {
            "type": "string",
            "description": "Whose charge it is."
          },
          "token_count_input": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Tokens in, on stage rows that have settled."
          },
          "token_count_output": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Tokens out, likewise."
          },
          "wall_ms": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "How long the stage ran."
          },
          "workspace_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The workspace it was incurred in — `null` once that workspace is\ndeleted, because a charge outlives what it was incurred on."
          }
        }
      },
      "ChargesResponse": {
        "type": "object",
        "description": "`GET /v1/tenants/me/billing/charges` — one page of the ledger.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ChargeSummary"
            },
            "description": "The rows on this page, newest first."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the next page resumes, or `null` on the last page — emitted\neither way, for the module's null rule."
          }
        }
      },
      "ConnectionView": {
        "type": "object",
        "description": "One provider's connection, as a status read returns it.\n\nThis response carries no access token and no expiry. It tells you whether\nsomeone connected this provider, and what the connection is called.",
        "required": [
          "status"
        ],
        "properties": {
          "label": {
            "type": [
              "string",
              "null"
            ],
            "description": "What a person sees the connection called, when the grant named one."
          },
          "status": {
            "type": "string",
            "description": "Whether this workspace holds a landed grant — [`STATUS_CONNECTED`] or\n[`STATUS_NOT_CONNECTED`]."
          }
        }
      },
      "ConsentRedirect": {
        "type": "object",
        "description": "`POST …/connectors/{provider}/connect` — where a person is sent to consent.",
        "required": [
          "install_url"
        ],
        "properties": {
          "install_url": {
            "type": "string",
            "description": "The provider's own page, carrying this round-trip's signed state."
          }
        }
      },
      "CreateModelEntryRequest": {
        "type": "object",
        "description": "Registers a model against a stored credential.\n\nagentsfleet ignores fields it does not know, instead of refusing the\nrequest.",
        "required": [
          "model_id",
          "secret_ref"
        ],
        "properties": {
          "model_id": {
            "type": "string",
            "description": "The model to register."
          },
          "secret_ref": {
            "type": "string",
            "description": "The credential to back it with. Immutable once the entry exists."
          }
        }
      },
      "CreateWorkspaceRequest": {
        "type": "object",
        "description": "`POST /v1/workspaces` — create one.",
        "properties": {
          "name": {
            "type": [
              "string",
              "null"
            ],
            "description": "What the workspace will be called, when the caller cares."
          }
        }
      },
      "CreatedWorkspaceResponse": {
        "type": "object",
        "description": "What creating answers with.",
        "required": [
          "workspace_id",
          "name",
          "request_id",
          "tenant_id"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Its name — echoed when chosen, revealed when generated."
          },
          "request_id": {
            "type": "string",
            "description": "The correlation token for this request, repeated in the body."
          },
          "tenant_id": {
            "type": "string",
            "description": "The tenant it was created in — the daemon's resolution, never a claim."
          },
          "workspace_id": {
            "type": "string",
            "description": "The new workspace's identifier."
          }
        }
      },
      "DeleteAllSessionsResponse": {
        "type": "object",
        "description": "What aborting every in-flight login answers with.",
        "required": [
          "aborted_count"
        ],
        "properties": {
          "aborted_count": {
            "type": "integer",
            "description": "How many were aborted by this call.",
            "minimum": 0
          }
        }
      },
      "EventDetail": {
        "type": "object",
        "description": "One event as the expanded view renders it — bodies included.",
        "required": [
          "fleet_id",
          "event_id",
          "workspace_id",
          "actor",
          "event_type",
          "status",
          "request_json",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "actor": {
            "type": "string",
            "description": "Who or what produced the event."
          },
          "checkpoint_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The session checkpoint this run wrote, when it wrote one."
          },
          "cost_nanos": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "What this event actually cost, summed over its telemetry rows."
          },
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds the row was created."
          },
          "event_id": {
            "type": "string",
            "description": "The canonical event identifier — the stream entry id that produced it."
          },
          "event_type": {
            "type": "string",
            "description": "How the event entered the system, as stored."
          },
          "failure_detail": {
            "type": [
              "string",
              "null"
            ],
            "description": "The operator-readable cause line, absent when none was carried."
          },
          "failure_label": {
            "type": [
              "string",
              "null"
            ],
            "description": "What refused or failed the run, absent on a clean one."
          },
          "fleet_id": {
            "type": "string",
            "description": "The fleet this event belongs to."
          },
          "request_json": {
            "type": "string",
            "description": "The trigger payload as stored, serialized to text."
          },
          "response_text": {
            "type": [
              "string",
              "null"
            ],
            "description": "The agent's full answer.\n\n`null` while a run is in flight, and on a run that failed before\nproducing one."
          },
          "resumes_event_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The event this one continues, set on a continuation."
          },
          "status": {
            "type": "string",
            "description": "Where the event's run got to, as stored."
          },
          "tokens": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Tokens the run spent, absent until a runner reports."
          },
          "updated_at": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds the row last changed."
          },
          "wall_ms": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Wall milliseconds the run took, absent until a runner reports."
          },
          "workspace_id": {
            "type": "string",
            "description": "The workspace the fleet belongs to."
          }
        }
      },
      "EventSummary": {
        "type": "object",
        "description": "One event as the operator surface renders it.",
        "required": [
          "fleet_id",
          "event_id",
          "workspace_id",
          "actor",
          "event_type",
          "status",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "actor": {
            "type": "string",
            "description": "Who or what produced the event."
          },
          "checkpoint_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The session checkpoint this run wrote, when it wrote one."
          },
          "cost_nanos": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "What this event actually cost, summed over its telemetry rows."
          },
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds the row was created."
          },
          "event_id": {
            "type": "string",
            "description": "The canonical event identifier — the stream entry id that produced it."
          },
          "event_type": {
            "type": "string",
            "description": "How the event entered the system, as stored."
          },
          "failure_detail": {
            "type": [
              "string",
              "null"
            ],
            "description": "The operator-readable cause line, absent when none was carried."
          },
          "failure_label": {
            "type": [
              "string",
              "null"
            ],
            "description": "What refused or failed the run, absent on a clean one."
          },
          "fleet_id": {
            "type": "string",
            "description": "The fleet this event belongs to."
          },
          "resumes_event_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The event this one continues, set on a continuation."
          },
          "status": {
            "type": "string",
            "description": "Where the event's run got to, as stored."
          },
          "tokens": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Tokens the run spent, absent until a runner reports."
          },
          "updated_at": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds the row last changed."
          },
          "wall_ms": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Wall milliseconds the run took, absent until a runner reports."
          },
          "workspace_id": {
            "type": "string",
            "description": "The workspace the fleet belongs to."
          }
        }
      },
      "EventsResponse": {
        "type": "object",
        "description": "A page of event history.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EventSummary"
            },
            "description": "The events on this page, newest first."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the next page resumes, or `null` on the last one."
          }
        }
      },
      "ExtraBind": {
        "type": "object",
        "description": "One host path bound into every lease's sandbox, in addition to the baseline.\n\nAn operator may ADD a path a host needs; never remove or re-mode one the\nsandbox depends on.",
        "required": [
          "path",
          "mode",
          "note"
        ],
        "properties": {
          "mode": {
            "$ref": "#/components/schemas/BindMode",
            "description": "Whether the bind is writable."
          },
          "note": {
            "type": "string",
            "description": "Operator note explaining why the bind exists."
          },
          "path": {
            "type": "string",
            "description": "Host path to bind."
          }
        }
      },
      "FailureClass": {
        "type": "string",
        "description": "Why a run failed, at the granularity the classification site knows.",
        "enum": [
          "startup_posture",
          "policy_deny",
          "timeout_kill",
          "oom_kill",
          "resource_kill",
          "runner_crash",
          "transport_loss",
          "landlock_deny",
          "lease_expired",
          "renewal_terminate",
          "budget_breach"
        ]
      },
      "Fired": {
        "type": "object",
        "description": "What an accepted schedule fire is answered with.",
        "required": [
          "event_id",
          "replayed"
        ],
        "properties": {
          "event_id": {
            "type": "string",
            "description": "The event the fleet will run, or already ran."
          },
          "replayed": {
            "type": "boolean",
            "description": "Whether an earlier attempt already produced it."
          }
        }
      },
      "FleetBundleItem": {
        "type": "object",
        "description": "One public Fleet Bundle gallery row.",
        "required": [
          "id",
          "name",
          "description",
          "required_credentials",
          "required_credentials_reasons",
          "required_tools",
          "network_hosts"
        ],
        "properties": {
          "description": {
            "type": "string",
            "description": "Curated summary."
          },
          "id": {
            "type": "string",
            "description": "Stable catalogue slug."
          },
          "name": {
            "type": "string",
            "description": "Display name."
          },
          "network_hosts": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Declared outbound hosts."
          },
          "required_credentials": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Credential names, never values."
          },
          "required_credentials_reasons": {
            "description": "Install-gate explanation keyed by credential name."
          },
          "required_tools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Required tool identifiers."
          }
        },
        "additionalProperties": false
      },
      "FleetBundlesResponse": {
        "type": "object",
        "description": "Public Fleet Bundle gallery response.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FleetBundleItem"
            },
            "description": "Every published row carrying current bundle content."
          }
        },
        "additionalProperties": false
      },
      "FleetDetailResponse": {
        "type": "object",
        "description": "`GET /v1/workspaces/{workspace_id}/fleets/{fleet_id}` — one fleet, whole.\n\nThe list row's fields plus the editable surface. Flattened rather than\nnested under a `summary` key, because that is the shape the source editor\nalready reads.",
        "required": [
          "id",
          "name",
          "status",
          "source_markdown",
          "events_processed",
          "budget_used_nanos",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "budget_used_nanos": {
            "type": "integer",
            "format": "int64",
            "description": "Lifetime spend, in nanos."
          },
          "bundle_content_hash": {
            "type": [
              "string",
              "null"
            ],
            "description": "The bundle a runner materialises support files from."
          },
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was installed."
          },
          "events_processed": {
            "type": "integer",
            "format": "int64",
            "description": "Lifetime event count."
          },
          "id": {
            "type": "string",
            "description": "The fleet's identifier."
          },
          "name": {
            "type": "string",
            "description": "Its name in this workspace."
          },
          "source_markdown": {
            "type": "string",
            "description": "The authored `SKILL.md`, verbatim."
          },
          "status": {
            "type": "string",
            "description": "Where it stands in its life."
          },
          "trigger_markdown": {
            "type": [
              "string",
              "null"
            ],
            "description": "The authored `TRIGGER.md`, or `null` where the bundle carried none."
          },
          "triggers": {
            "type": [
              "object",
              "null"
            ],
            "description": "What may wake this Fleet, as stored. agentsfleet returns the document\nunchanged."
          },
          "updated_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it last changed."
          }
        }
      },
      "FleetResponseChunk": {
        "type": "object",
        "description": "The fleet produced output.",
        "required": [
          "text"
        ],
        "properties": {
          "text": {
            "type": "string",
            "description": "The text produced."
          }
        },
        "additionalProperties": false
      },
      "FleetSummary": {
        "type": "object",
        "description": "One fleet as a list page shows it.",
        "required": [
          "id",
          "name",
          "status",
          "created_at",
          "updated_at",
          "events_processed",
          "budget_used_nanos"
        ],
        "properties": {
          "budget_used_nanos": {
            "type": "integer",
            "format": "int64",
            "description": "Lifetime spend, in nanos."
          },
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was installed."
          },
          "events_processed": {
            "type": "integer",
            "format": "int64",
            "description": "Lifetime event count. Server truth, never client arithmetic."
          },
          "id": {
            "type": "string",
            "description": "The fleet's identifier."
          },
          "name": {
            "type": "string",
            "description": "Its name in this workspace."
          },
          "status": {
            "type": "string",
            "description": "Where it stands in its life."
          },
          "triggers": {
            "type": [
              "object",
              "null"
            ],
            "description": "What may wake this Fleet, as stored. agentsfleet returns the document\nunchanged."
          },
          "updated_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it last changed."
          }
        }
      },
      "FleetsResponse": {
        "type": "object",
        "description": "`GET /v1/workspaces/{workspace_id}/fleets` — one page, newest first.",
        "required": [
          "items",
          "total"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FleetSummary"
            },
            "description": "The fleets on this page."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the next page resumes, or `null` on the last one."
          },
          "total": {
            "type": "integer",
            "description": "How many Fleets this page carries. The count covers this page only, not\nthe whole workspace.",
            "minimum": 0
          }
        }
      },
      "GalleryCard": {
        "type": "object",
        "description": "One gallery card.",
        "required": [
          "id",
          "name",
          "description",
          "visibility",
          "source_ref",
          "created_at",
          "requirements",
          "required_credentials_reasons"
        ],
        "properties": {
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was onboarded, in epoch milliseconds."
          },
          "description": {
            "type": "string",
            "description": "The summary from the bundle's own frontmatter."
          },
          "id": {
            "type": "string",
            "description": "The entry's identifier — a slug from the platform tier, a UUID from the\nworkspace's own. Opaque to a caller either way."
          },
          "name": {
            "type": "string",
            "description": "The display name."
          },
          "required_credentials_reasons": {
            "description": "Per-credential \"why this fleet needs it\" copy, keyed by name."
          },
          "requirements": {
            "$ref": "#/components/schemas/AdminLibraryRequirements",
            "description": "Credential, tool and host names. Never values."
          },
          "source_ref": {
            "type": "string",
            "description": "The repository or template it was onboarded from."
          },
          "visibility": {
            "type": "string",
            "description": "Which library the card came from: `platform` or `tenant`."
          }
        },
        "additionalProperties": false
      },
      "GalleryResponse": {
        "type": "object",
        "description": "`GET /v1/workspaces/{workspace_id}/fleet-libraries` — one page of the gallery.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GalleryCard"
            },
            "description": "The cards, newest first, platform before tenant within an instant."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the next page resumes, or null on the last one."
          },
          "total": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Always null.",
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "GrantSummary": {
        "type": "object",
        "description": "One grant as the fleet's list shows it.",
        "required": [
          "id",
          "service",
          "status",
          "created_at",
          "reason"
        ],
        "properties": {
          "approved_at": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "When a person approved it, or `null`."
          },
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When the grant was raised."
          },
          "id": {
            "type": "string",
            "description": "The grant's own row id — what a revoke addresses it by."
          },
          "reason": {
            "type": "string",
            "description": "Why the row exists at all — the install's own words, stored once."
          },
          "revoked_at": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "When a person took that back, or `null`."
          },
          "service": {
            "type": "string",
            "description": "The third party the grant is about, as the credential classifier names it."
          },
          "status": {
            "type": "string",
            "description": "Where the decision stands, from [`status`]."
          }
        }
      },
      "GrantsResponse": {
        "type": "object",
        "description": "`GET /v1/workspaces/{workspace_id}/fleets/{fleet_id}/integration-grants`.",
        "required": [
          "items",
          "total"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GrantSummary"
            },
            "description": "The fleet's grants, newest first."
          },
          "total": {
            "type": "integer",
            "description": "How many rows `items` carries.",
            "minimum": 0
          }
        }
      },
      "HeartbeatResponse": {
        "type": "object",
        "description": "`POST /v1/runners/me/heartbeats` reply.\n\nCarries the current assignment on EVERY beat, so a dashboard change reaches\nthe host within one interval. A null assignment means a row predating the\npolicy columns: the runner then fails closed and refuses to lease.",
        "required": [
          "status",
          "degraded",
          "selftest_requested"
        ],
        "properties": {
          "assigned_policy": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/AssignedPolicy",
                "description": "The runner's current assignment."
              }
            ]
          },
          "degraded": {
            "type": "boolean",
            "description": "Whether the row reads degraded."
          },
          "degraded_reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why it reads degraded."
          },
          "selftest_requested": {
            "type": "boolean",
            "description": "An operator asked this runner to self-test. Rides the beat like the\nassignment does — one interval, no second endpoint, no host visit."
          },
          "status": {
            "$ref": "#/components/schemas/HeartbeatStatus",
            "description": "Whether to keep working, drain, or stop."
          }
        },
        "additionalProperties": false
      },
      "HeartbeatStatus": {
        "type": "string",
        "description": "Heartbeat reply status.",
        "enum": [
          "ok",
          "drain",
          "stop"
        ]
      },
      "Ignored": {
        "type": "object",
        "description": "What a delivery this daemon deliberately dropped is answered with.\n\n`200` and a reason, never a 4xx. Every one of these is a real,\ncorrectly-signed delivery that simply does not wake this fleet — a green\nbuild, a label edit, a paused fleet. Answering an error would put it in the\nsender's retry queue forever, and retrying changes none of them.",
        "required": [
          "ignored"
        ],
        "properties": {
          "ignored": {
            "type": "string",
            "description": "Which rule dropped it."
          }
        }
      },
      "InstallFleetRequest": {
        "type": "object",
        "description": "`POST /v1/workspaces/{workspace_id}/fleets` — install one.",
        "properties": {
          "name": {
            "type": [
              "string",
              "null"
            ],
            "description": "The operator's name for this instance, when they want one.\n\nThe same bundle backs many fleets in a workspace, each with its own name\nand its own webhooks. Absent means the bundle's declared name."
          },
          "platform_library_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "A published platform entry, by slug."
          },
          "tenant_library_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "This workspace's own entry, by identifier."
          }
        }
      },
      "InstalledFleetResponse": {
        "type": "object",
        "description": "What an install answers with.",
        "required": [
          "fleet_id",
          "name",
          "status",
          "webhook_urls"
        ],
        "properties": {
          "fleet_id": {
            "type": "string",
            "description": "The new fleet's identifier."
          },
          "name": {
            "type": "string",
            "description": "The name it was stored under — the drawn one, when a default collided."
          },
          "status": {
            "type": "string",
            "description": "Where it stands. `active`: the stream exists, so it is already leasable."
          },
          "webhook_urls": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WebhookUrl"
            },
            "description": "One entry per webhook trigger the bundle declared, keyed by source.\n\nAn empty object where the fleet declares none, never `null`: a client\niterating the map should not have to branch on its absence first."
          }
        }
      },
      "MemoriesResponse": {
        "type": "object",
        "description": "`GET /v1/workspaces/{workspace_id}/fleets/{fleet_id}/memories` — one page.",
        "required": [
          "items",
          "total"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MemoryEntry"
            },
            "description": "The entries on this page, newest first."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the next page resumes, or `null` on the last one."
          },
          "total": {
            "type": "integer",
            "description": "How many memories this page carries. The count covers this page only,\nnot the whole Fleet.",
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "MemoryCaptureResponse": {
        "type": "object",
        "description": "`POST /v1/runners/me/memory/{fleet_id}` reply — what the write did.",
        "required": [
          "stored",
          "skipped"
        ],
        "properties": {
          "skipped": {
            "type": "integer",
            "description": "Deltas refused for shape, which the runner should investigate.",
            "minimum": 0
          },
          "stored": {
            "type": "integer",
            "description": "Deltas written, after upsert.",
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "MemoryDelta": {
        "type": "object",
        "description": "One durable memory item — the unit of both reading and writing.",
        "required": [
          "key",
          "content",
          "category"
        ],
        "properties": {
          "category": {
            "type": "string",
            "description": "Retention category, which decides eviction order."
          },
          "content": {
            "type": "string",
            "description": "The remembered content."
          },
          "key": {
            "type": "string",
            "description": "Stable key. A repeated key overwrites rather than accumulating."
          }
        },
        "additionalProperties": false
      },
      "MemoryEntry": {
        "type": "object",
        "description": "One stored entry as the OPERATOR surface renders it.",
        "required": [
          "key",
          "content",
          "category",
          "updated_at"
        ],
        "properties": {
          "category": {
            "type": "string",
            "description": "The retention category, which decides eviction order."
          },
          "content": {
            "type": "string",
            "description": "What it remembers."
          },
          "key": {
            "type": "string",
            "description": "The stable key the fleet remembers this under."
          },
          "updated_at": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds, as a JSON NUMBER — never a decimal string."
          }
        },
        "additionalProperties": false
      },
      "MemoryHydrateResponse": {
        "type": "object",
        "description": "What a fleet remembers, compacted to fit one window.",
        "required": [
          "memory"
        ],
        "properties": {
          "memory": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MemoryDelta"
            },
            "description": "The window's items."
          }
        },
        "additionalProperties": false
      },
      "MemoryPushRequest": {
        "type": "object",
        "description": "`POST /v1/runners/me/memory/{fleet_id}` request.",
        "required": [
          "lease_id",
          "fencing_token",
          "memory"
        ],
        "properties": {
          "fencing_token": {
            "type": "integer",
            "format": "int64",
            "description": "Monotonic guard; a reclaimed holder is rejected.",
            "minimum": 0
          },
          "lease_id": {
            "type": "string",
            "description": "The lease authorizing this write."
          },
          "memory": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MemoryDelta"
            },
            "description": "The items to remember."
          }
        }
      },
      "MintApiKeyRequest": {
        "type": "object",
        "description": "`POST /v1/api-keys` — mint one tenant credential.",
        "required": [
          "key_name"
        ],
        "properties": {
          "description": {
            "type": [
              "string",
              "null"
            ],
            "description": "Free text beside it, for whoever reads the list in six months."
          },
          "key_name": {
            "type": "string",
            "description": "What the key will be called. Unique per tenant."
          }
        },
        "additionalProperties": false
      },
      "MintCliCredentialRequest": {
        "type": "object",
        "description": "`POST /v1/cli-credentials` — mint this machine's credential.",
        "required": [
          "machine_name"
        ],
        "properties": {
          "machine_name": {
            "type": "string",
            "description": "The terminal's own label, as an operator will read it back."
          }
        }
      },
      "MintCredentialRequest": {
        "type": "object",
        "description": "`POST /v1/runners/me/credentials/mint` request.\n\nThe runner forwards the sandboxed child's ask verbatim. `lease_id` binds the\nmint to the lease's workspace SERVER-SIDE — the child never names a\nworkspace, so a prompt-injected child cannot mint for another tenant. The\nrequest carries no token.",
        "required": [
          "lease_id",
          "integration"
        ],
        "properties": {
          "integration": {
            "type": "string",
            "description": "Which connected integration to mint for."
          },
          "lease_id": {
            "type": "string",
            "description": "The lease the mint is bound to."
          },
          "scope": {
            "type": [
              "string",
              "null"
            ],
            "description": "An integration-specific narrowing the broker may honour."
          }
        },
        "additionalProperties": false
      },
      "MintCredentialResponse": {
        "type": "object",
        "description": "`POST /v1/runners/me/credentials/mint` reply.",
        "required": [
          "token",
          "expires_at_ms"
        ],
        "properties": {
          "expires_at_ms": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds after which it stops working, so the caller re-mints\nbefore expiry rather than on failure."
          },
          "token": {
            "type": "string",
            "description": "The short-lived, workspace-scoped credential. Secret — never logged,\nnever echoed into a frame."
          }
        },
        "additionalProperties": false
      },
      "MintedApiKeyResponse": {
        "type": "object",
        "description": "The one response that reveals a key's plaintext.",
        "required": [
          "id",
          "key_name",
          "key",
          "created_at"
        ],
        "properties": {
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was minted."
          },
          "id": {
            "type": "string",
            "description": "The key's identifier, which every later call addresses it by."
          },
          "key": {
            "type": "string",
            "description": "The credential itself. Shown once and never stored in this shape."
          },
          "key_name": {
            "type": "string",
            "description": "What it is called."
          }
        }
      },
      "MintedCliCredentialResponse": {
        "type": "object",
        "description": "The only response that returns a command-line credential in plaintext.\n\nSave the value when you receive it. No later call returns it again.",
        "required": [
          "id",
          "credential",
          "machine_name",
          "deployment"
        ],
        "properties": {
          "credential": {
            "type": "string",
            "description": "The credential itself. Shown once and never stored in this shape."
          },
          "deployment": {
            "type": "string",
            "description": "The deployment that minted it — this daemon, never a caller's claim."
          },
          "id": {
            "type": "string",
            "description": "The credential row's identifier, which the revoke addresses it by."
          },
          "machine_name": {
            "type": "string",
            "description": "The terminal's label, echoed back as it was stored."
          }
        }
      },
      "ModelEntriesResponse": {
        "type": "object",
        "description": "`GET /v1/tenants/me/models` — one page of the registry.",
        "required": [
          "models",
          "platform_default_available"
        ],
        "properties": {
          "models": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ModelEntryRow"
            },
            "description": "The registered models, newest first."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the next page resumes, or null on the last one."
          },
          "platform_default": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PlatformDefaultRow",
                "description": "The default's identity, when there is one."
              }
            ]
          },
          "platform_default_available": {
            "type": "boolean",
            "description": "Whether an active platform-default row exists at all."
          },
          "total": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Always null — see the module note.",
            "minimum": 0
          }
        }
      },
      "ModelEntryRow": {
        "type": "object",
        "description": "One configured model, as the registry page shows it.\n\n`id` is the entry's own identity — the `{id}` the item route takes — and not\nthe model's. The model is `model_id`, which is what a provider spells.",
        "required": [
          "id",
          "model_id",
          "secret_ref",
          "kind",
          "has_key",
          "active",
          "created_at"
        ],
        "properties": {
          "active": {
            "type": "boolean",
            "description": "Whether this entry is the tenant's current selection."
          },
          "base_url": {
            "type": [
              "string",
              "null"
            ],
            "description": "The custom endpoint, where one may be displayed."
          },
          "cached_input_nanos_per_mtok": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "The cached-input rate, likewise."
          },
          "context_cap_tokens": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "The context window this model is priced at, in tokens.\n\nAbsent when the catalogue carries no rate for this model. An absent\nvalue does not mean zero.",
            "minimum": 0
          },
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was first stored, in epoch milliseconds."
          },
          "has_key": {
            "type": "boolean",
            "description": "Whether a key is stored under that name. Never the key."
          },
          "id": {
            "type": "string",
            "description": "The entry's identity."
          },
          "input_nanos_per_mtok": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "The input rate, in nanos per million tokens."
          },
          "kind": {
            "type": "string",
            "description": "What the credential is, as the server classified it at write time."
          },
          "model_id": {
            "type": "string",
            "description": "The model this entry configures."
          },
          "output_nanos_per_mtok": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "The output rate, likewise."
          },
          "provider": {
            "type": [
              "string",
              "null"
            ],
            "description": "The provider label, for the credential kinds that carry one."
          },
          "secret_ref": {
            "type": "string",
            "description": "The vault key name backing it."
          }
        }
      },
      "ModelRates": {
        "type": "object",
        "description": "Mutable rate fields shared by admin model create and patch.",
        "required": [
          "context_cap_tokens",
          "input_nanos_per_mtok",
          "cached_input_nanos_per_mtok",
          "output_nanos_per_mtok"
        ],
        "properties": {
          "cached_input_nanos_per_mtok": {
            "type": "integer",
            "format": "int64",
            "description": "Cached-input nanos per million tokens."
          },
          "context_cap_tokens": {
            "type": "integer",
            "format": "int32",
            "description": "Maximum context tokens."
          },
          "input_nanos_per_mtok": {
            "type": "integer",
            "format": "int64",
            "description": "Input-token nanos per million tokens."
          },
          "output_nanos_per_mtok": {
            "type": "integer",
            "format": "int64",
            "description": "Output-token nanos per million tokens."
          }
        },
        "additionalProperties": false
      },
      "NetworkPolicy": {
        "type": "string",
        "description": "Egress posture assigned per runner, named so the behaviour reads off the value.",
        "enum": [
          "allow_all",
          "deny_all_egress",
          "allow_list_egress"
        ]
      },
      "OnboardingResponse": {
        "type": "object",
        "description": "The whole onboarding checklist, in one call.",
        "required": [
          "model_configured",
          "has_fleet",
          "has_secret",
          "has_processed_event",
          "has_steer_event",
          "cli_ticked",
          "dismissed",
          "collapsed"
        ],
        "properties": {
          "cli_ticked": {
            "type": "boolean",
            "description": "The person ticked the install-the-CLI step by hand."
          },
          "collapsed": {
            "type": "boolean",
            "description": "The panel is collapsed but not dismissed."
          },
          "dismissed": {
            "type": "boolean",
            "description": "The person dismissed the panel outright."
          },
          "has_fleet": {
            "type": "boolean",
            "description": "The workspace holds at least one fleet."
          },
          "has_processed_event": {
            "type": "boolean",
            "description": "At least one event has reached the workspace."
          },
          "has_secret": {
            "type": "boolean",
            "description": "The workspace holds at least one vault secret."
          },
          "has_steer_event": {
            "type": "boolean",
            "description": "At least one of those events came from a steer."
          },
          "model_configured": {
            "type": "boolean",
            "description": "A model resolves — the tenant's own selection, or the platform default."
          }
        }
      },
      "OpenSessionRequest": {
        "type": "object",
        "description": "`POST /v1/auth/sessions` — the command line opens a login.",
        "required": [
          "public_key",
          "token_name"
        ],
        "properties": {
          "public_key": {
            "type": "string",
            "description": "The client's public key. Relayed; this daemon runs no curve over it."
          },
          "token_name": {
            "type": "string",
            "description": "What the credential this login mints will be called."
          }
        },
        "additionalProperties": false
      },
      "OpenSessionResponse": {
        "type": "object",
        "description": "What opening a login answers with.",
        "required": [
          "session_id",
          "login_url",
          "request_id"
        ],
        "properties": {
          "login_url": {
            "type": "string",
            "description": "The page a person approves the login on."
          },
          "request_id": {
            "type": "string",
            "description": "The request that opened it, for an operator correlating a failed login."
          },
          "session_id": {
            "type": "string",
            "description": "The identifier the command line polls on."
          }
        }
      },
      "Outcome": {
        "type": "string",
        "description": "The terminal verdict a runner reports.\n\nMirrors the event statuses a RUNNER can produce; the daemon-side statuses are\nnever runner-reported.",
        "enum": [
          "processed",
          "fleet_error"
        ]
      },
      "Page": {
        "type": "object",
        "description": "A page of schedules.",
        "required": [
          "schedules"
        ],
        "properties": {
          "schedules": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/View"
            },
            "description": "The fleet's schedules, oldest first."
          }
        }
      },
      "PageResponse_ApiKeySummary": {
        "type": "object",
        "description": "One page of a list, in the envelope every list on this API shares.\n\nEach page carries three keys: `items`, `total` and `next_cursor`. A\n`next_cursor` of `null` means you have reached the last page.",
        "required": [
          "items",
          "total"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "description": "One key as a list shows it: metadata, never a secret.",
              "required": [
                "id",
                "key_name",
                "active",
                "created_at"
              ],
              "properties": {
                "active": {
                  "type": "boolean",
                  "description": "Whether it still authenticates."
                },
                "created_at": {
                  "type": "integer",
                  "format": "int64",
                  "description": "When it was minted."
                },
                "id": {
                  "type": "string",
                  "description": "The key's identifier."
                },
                "key_name": {
                  "type": "string",
                  "description": "What it is called."
                },
                "last_used_at": {
                  "type": [
                    "integer",
                    "null"
                  ],
                  "format": "int64",
                  "description": "When it last authenticated, if it ever has."
                },
                "revoked_at": {
                  "type": [
                    "integer",
                    "null"
                  ],
                  "format": "int64",
                  "description": "When it stopped working, if it has.\n\nEmitted as `null` rather than omitted — here and on every optional in\nthis module. The Zig daemon serialises through `res.json(value, .{})`,\nand std.json's default emits null optionals, so a row is always the\nsame set of keys. Omission would be a shape change a dashboard's\n`\"revoked_at\" in row` check can feel."
                }
              }
            },
            "description": "The rows on this page, in the requested order."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the next page resumes, or `null` on the last page.\n\nAlways emitted, never omitted, for the module's null rule."
          },
          "total": {
            "type": "integer",
            "format": "int64",
            "description": "How many rows the collection holds in total.\n\nPage-stable — the count carries no keyset predicate — so a client\nwalking pages sees one number rather than a shrinking one. Whether more\npages exist is answered by `next_cursor` being non-null, not by a flag."
          }
        }
      },
      "PatchApiKeyRequest": {
        "type": "object",
        "description": "`PATCH /v1/api-keys/{id}` — the only mutation, and only downward.",
        "required": [
          "active"
        ],
        "properties": {
          "active": {
            "type": "boolean",
            "description": "Must be `false`. A revoked key is never brought back."
          }
        },
        "additionalProperties": false
      },
      "PatchedFleetResponse": {
        "oneOf": [
          {
            "type": "object",
            "description": "An empty body: nothing was written, and no row was even read.",
            "required": [
              "fleet_id"
            ],
            "properties": {
              "config_revision": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "int64",
                "description": "Always `null` on this response. Use it to tell \"no write\" apart\nfrom a revision number."
              },
              "fleet_id": {
                "type": "string",
                "description": "The fleet the request named."
              }
            }
          },
          {
            "type": "object",
            "description": "A configuration or source edit, with no transition asked for.",
            "required": [
              "fleet_id",
              "config_revision",
              "etag"
            ],
            "properties": {
              "config_revision": {
                "type": "integer",
                "format": "int64",
                "description": "The new revision — `updated_at`, echoed back by a conditional caller."
              },
              "etag": {
                "type": "string",
                "description": "The tag over the post-update source, for the editor's next save."
              },
              "fleet_id": {
                "type": "string",
                "description": "The fleet that changed."
              }
            }
          },
          {
            "type": "object",
            "description": "A transition, with or without an edit beside it.",
            "required": [
              "fleet_id",
              "status",
              "config_revision",
              "etag"
            ],
            "properties": {
              "config_revision": {
                "type": "integer",
                "format": "int64",
                "description": "The new revision."
              },
              "etag": {
                "type": "string",
                "description": "The tag over the post-update source."
              },
              "fleet_id": {
                "type": "string",
                "description": "The fleet that changed."
              },
              "status": {
                "type": "string",
                "description": "The status it now holds."
              }
            }
          }
        ],
        "description": "What a PATCH answers with — one of three shapes."
      },
      "PlatformDefaultRow": {
        "type": "object",
        "description": "The deployment's platform default, as the page's Default row shows it.",
        "required": [
          "provider",
          "model",
          "context_cap_tokens"
        ],
        "properties": {
          "cached_input_nanos_per_mtok": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "The cached-input rate, likewise."
          },
          "context_cap_tokens": {
            "type": "integer",
            "format": "int32",
            "description": "The context window the catalogue prices it at.",
            "minimum": 0
          },
          "input_nanos_per_mtok": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "The input rate, in nanos per million tokens."
          },
          "model": {
            "type": "string",
            "description": "The model it selects."
          },
          "output_nanos_per_mtok": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "The output rate, likewise."
          },
          "provider": {
            "type": "string",
            "description": "The provider the default belongs to."
          }
        }
      },
      "PlatformKeyDeactivateResponse": {
        "type": "object",
        "description": "Successful platform-default deactivation.",
        "required": [
          "provider",
          "active",
          "request_id"
        ],
        "properties": {
          "active": {
            "type": "boolean",
            "description": "Always false after deactivation."
          },
          "provider": {
            "type": "string",
            "description": "Provider that was deactivated."
          },
          "request_id": {
            "type": "string",
            "description": "Server-generated support correlation id."
          }
        },
        "additionalProperties": false
      },
      "PlatformKeyItem": {
        "type": "object",
        "description": "Reveal-free platform-key list item.",
        "required": [
          "provider",
          "source_workspace_id",
          "active",
          "updated_at"
        ],
        "properties": {
          "active": {
            "type": "boolean",
            "description": "Whether this row is the platform default."
          },
          "model": {
            "type": [
              "string",
              "null"
            ],
            "description": "Active priced model, absent after deactivation."
          },
          "provider": {
            "type": "string",
            "description": "Provider and vault-row name."
          },
          "source_workspace_id": {
            "type": "string",
            "description": "Workspace holding the key."
          },
          "updated_at": {
            "type": "integer",
            "format": "int64",
            "description": "Last mutation instant in epoch milliseconds."
          }
        },
        "additionalProperties": false
      },
      "PlatformKeySetResponse": {
        "type": "object",
        "description": "Successful platform-default activation.",
        "required": [
          "provider",
          "model",
          "source_workspace_id",
          "active",
          "request_id"
        ],
        "properties": {
          "active": {
            "type": "boolean",
            "description": "Always true after activation."
          },
          "model": {
            "type": "string",
            "description": "Selected priced model."
          },
          "provider": {
            "type": "string",
            "description": "Provider and vault-row name."
          },
          "request_id": {
            "type": "string",
            "description": "Server-generated support correlation id."
          },
          "source_workspace_id": {
            "type": "string",
            "description": "Workspace holding the key."
          }
        },
        "additionalProperties": false
      },
      "PlatformKeysResponse": {
        "type": "object",
        "description": "`GET /v1/admin/platform-keys` response.",
        "required": [
          "keys",
          "request_id"
        ],
        "properties": {
          "keys": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlatformKeyItem"
            },
            "description": "Every active and inactive provider row."
          },
          "request_id": {
            "type": "string",
            "description": "Server-generated support correlation id."
          }
        },
        "additionalProperties": false
      },
      "PollSessionResponse": {
        "type": "object",
        "description": "`GET /v1/auth/sessions/{session_id}` — where a login has got to.",
        "required": [
          "status",
          "cli_public_key",
          "token_name",
          "expires_at_ms"
        ],
        "properties": {
          "cli_public_key": {
            "type": "string",
            "description": "The public key the command line presented, echoed back."
          },
          "expires_at_ms": {
            "type": "integer",
            "format": "int64",
            "description": "When the window closes, in milliseconds since the epoch."
          },
          "status": {
            "type": "string",
            "description": "`pending` or `verification_pending`."
          },
          "token_name": {
            "type": "string",
            "description": "What the credential will be called."
          }
        }
      },
      "Pong": {
        "type": "object",
        "description": "What a `ping` is answered with.",
        "required": [
          "status"
        ],
        "properties": {
          "status": {
            "type": "string",
            "description": "The pong marker this deployment answers with."
          }
        }
      },
      "PreferencesResponse": {
        "type": "object",
        "description": "Every preference you have set in this workspace.\n\nA write returns the whole set, including the keys it did not change.",
        "required": [
          "prefs"
        ],
        "properties": {
          "prefs": {
            "type": "object",
            "description": "Every key you have set in this workspace, ordered by key.\n\nEach value is any well-formed JSON value, returned as it was stored.\nThe order is stable between two reads."
          }
        }
      },
      "ProviderMode": {
        "type": "string",
        "description": "The `mode` a client sends and reads back.",
        "enum": [
          "platform",
          "self_managed"
        ]
      },
      "RegisterResponse": {
        "type": "object",
        "description": "`POST /v1/runners` reply: the durable identity plus its bearer token.\n\nThe token is returned ONCE — the daemon stores only its hash.",
        "required": [
          "runner_id",
          "runner_token",
          "assigned_policy"
        ],
        "properties": {
          "assigned_policy": {
            "$ref": "#/components/schemas/AssignedPolicy",
            "description": "The assignment AS STORED, with the worker count clamped, so the\nenrolling operator sees exactly what the host will apply."
          },
          "runner_id": {
            "type": "string",
            "description": "The runner's durable identifier."
          },
          "runner_token": {
            "type": "string",
            "description": "The minted bearer token. Secret — revealed once, never re-readable."
          }
        },
        "additionalProperties": false
      },
      "RenewResponse": {
        "type": "object",
        "description": "`POST /v1/runners/me/leases/{lease_id}/renew` reply.\n\nThe authoritative new kill deadline. A non-`200` means stop renewing and kill\nthe child — the run is over.",
        "required": [
          "lease_expires_at"
        ],
        "properties": {
          "lease_expires_at": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds of the new deadline."
          }
        },
        "additionalProperties": false
      },
      "ReplaceSecretRequest": {
        "type": "object",
        "description": "`PUT /v1/workspaces/{workspace_id}/secrets/{secret_name}` — replace the body.",
        "required": [
          "data"
        ],
        "properties": {
          "data": {
            "type": "object",
            "description": "The complete replacement body.\n\nOpaque for the same reason create's is: an unparsed slice in Rust, an\nobject on the wire. `value_type` names the difference."
          }
        }
      },
      "ReportCheckpoint": {
        "type": "object",
        "description": "Session resume cursor written to the fleet's stored session context.",
        "required": [
          "last_event_id",
          "last_response"
        ],
        "properties": {
          "last_event_id": {
            "type": "string",
            "description": "The last event this session processed."
          },
          "last_response": {
            "type": "string",
            "description": "The last response it produced."
          }
        },
        "additionalProperties": false
      },
      "ReportRequest": {
        "type": "object",
        "description": "`POST /v1/runners/me/reports` — one batched write keyed by event id.",
        "required": [
          "lease_id",
          "event_id",
          "fencing_token",
          "outcome",
          "failure_detail",
          "response_text",
          "tokens",
          "input_tokens",
          "cached_input_tokens",
          "output_tokens",
          "telemetry",
          "checkpoint"
        ],
        "properties": {
          "cached_input_tokens": {
            "type": "integer",
            "format": "int32",
            "description": "Cumulative cache-read tokens for the whole run.",
            "minimum": 0
          },
          "checkpoint": {
            "$ref": "#/components/schemas/ReportCheckpoint",
            "description": "Where to resume this session."
          },
          "event_id": {
            "type": "string",
            "description": "The event this report is about."
          },
          "failure_detail": {
            "type": "string",
            "description": "Human-readable cause, stored only on failure."
          },
          "failure_reason": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/FailureClass",
                "description": "The granular cause when the run failed."
              }
            ]
          },
          "fencing_token": {
            "type": "integer",
            "format": "int64",
            "description": "Monotonic guard, verified against the fleet's live sequence.",
            "minimum": 0
          },
          "input_tokens": {
            "type": "integer",
            "format": "int32",
            "description": "Cumulative prompt tokens for the whole run.",
            "minimum": 0
          },
          "lease_id": {
            "type": "string",
            "description": "The lease being reported on."
          },
          "outcome": {
            "$ref": "#/components/schemas/Outcome",
            "description": "The binary verdict."
          },
          "output_tokens": {
            "type": "integer",
            "format": "int32",
            "description": "Cumulative completion tokens for the whole run.",
            "minimum": 0
          },
          "response_text": {
            "type": "string",
            "description": "The run's output."
          },
          "telemetry": {
            "$ref": "#/components/schemas/ReportTelemetry",
            "description": "Latency the runner observed."
          },
          "tokens": {
            "type": "integer",
            "format": "int64",
            "description": "The run's total tokens, for reporting. Billing charges the three\ncumulative fields below, which settle against the usage ledger.",
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "ReportResponse": {
        "type": "object",
        "description": "`POST /v1/runners/me/reports` reply.",
        "required": [
          "ok"
        ],
        "properties": {
          "ok": {
            "type": "boolean",
            "description": "Whether the write landed."
          }
        },
        "additionalProperties": false
      },
      "ReportTelemetry": {
        "type": "object",
        "description": "Latency the runner observed for one run.",
        "required": [
          "time_to_first_token_ms",
          "wall_ms"
        ],
        "properties": {
          "time_to_first_token_ms": {
            "type": "integer",
            "format": "int32",
            "description": "Milliseconds until the first token arrived.",
            "minimum": 0
          },
          "wall_ms": {
            "type": "integer",
            "format": "int64",
            "description": "Total wall-clock milliseconds.",
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "Resolved": {
        "type": "object",
        "description": "What the approval callback returns to the sender that resolved a gate.\n\nThis response repeats what the sender sent, plus a marker. It never reports\nwho else answered the gate.",
        "required": [
          "status",
          "action_id",
          "decision"
        ],
        "properties": {
          "action_id": {
            "type": "string",
            "description": "The gate that was answered."
          },
          "decision": {
            "type": "string",
            "description": "The answer it was given."
          },
          "status": {
            "type": "string",
            "description": "The resolved marker this route answers with."
          }
        }
      },
      "ResolvedResponse": {
        "type": "object",
        "description": "What answering a gate reports, whether or not this caller won the race.",
        "required": [
          "gate_id",
          "action_id",
          "outcome",
          "resolved_at",
          "resolved_by"
        ],
        "properties": {
          "action_id": {
            "type": "string",
            "description": "The action it gated."
          },
          "gate_id": {
            "type": "string",
            "description": "The gate that was answered."
          },
          "outcome": {
            "type": "string",
            "description": "Where it now stands."
          },
          "resolved_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was decided."
          },
          "resolved_by": {
            "type": "string",
            "description": "Who decided it — not necessarily this caller."
          }
        }
      },
      "RevokedApiKeyResponse": {
        "type": "object",
        "description": "What revoking answers with.",
        "required": [
          "id",
          "active",
          "revoked_at"
        ],
        "properties": {
          "active": {
            "type": "boolean",
            "description": "Always `false` — the field is present so a client can write the reply\nstraight into whatever it was holding."
          },
          "id": {
            "type": "string",
            "description": "The key's identifier."
          },
          "revoked_at": {
            "type": "integer",
            "format": "int64",
            "description": "When the row records it stopped working."
          }
        }
      },
      "RunnerAdminAction": {
        "type": "string",
        "description": "Platform-operator mutation actions.\n\nRotation and self-test are operations rather than state transitions. They\nride the same endpoint because they share the operator scope and the\nexactly-one-of body guard with the transition actions.",
        "enum": [
          "cordon",
          "drain",
          "revoke",
          "rotate",
          "self_test"
        ]
      },
      "RunnerAdminPatchRequest": {
        "type": "object",
        "description": "`PATCH /v1/fleets/runners/{id}` body.\n\nExactly one of `action` or `assigned_policy`; both or neither is a `400`.",
        "properties": {
          "action": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/RunnerAdminAction",
                "description": "A state transition, token rotation, or self-test request."
              }
            ]
          },
          "assigned_policy": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/AssignedPolicy",
                "description": "A policy re-assignment, which reaches the host on its next heartbeat."
              }
            ]
          }
        },
        "additionalProperties": false
      },
      "RunnerAdminPatchResponse": {
        "type": "object",
        "description": "`PATCH /v1/fleets/runners/{id}` reply.",
        "required": [
          "id",
          "admin_state"
        ],
        "properties": {
          "admin_state": {
            "$ref": "#/components/schemas/AdminState",
            "description": "Its state after the change."
          },
          "assigned_policy": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/AssignedPolicy",
                "description": "The assignment as stored, on the policy-update path."
              }
            ]
          },
          "id": {
            "type": "string",
            "description": "The runner that changed."
          },
          "selftest_requested_at": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "When the self-test request was recorded, epoch milliseconds.\n\nThe reply is the REQUEST, never a verdict — the daemon picks the ask up\non its next beat, so a result cannot exist yet. Returning the instant\nlets a reader age the pending state instead of spinning with no clock."
          }
        },
        "additionalProperties": false
      },
      "RunnerDetail": {
        "allOf": [
          {
            "$ref": "#/components/schemas/RunnerItem",
            "description": "The fields shared with the runner list, flattened on the wire."
          },
          {
            "type": "object",
            "required": [
              "active_lease_count",
              "active_fleet_count",
              "leases_acquired",
              "leases_succeeded",
              "leases_failed",
              "leases_expired"
            ],
            "properties": {
              "active_fleet_count": {
                "type": "integer",
                "format": "int64",
                "description": "Fleets represented by live leases."
              },
              "active_lease_count": {
                "type": "integer",
                "format": "int64",
                "description": "Live leases currently held."
              },
              "leases_acquired": {
                "type": "integer",
                "format": "int64",
                "description": "Lifetime acquired count."
              },
              "leases_expired": {
                "type": "integer",
                "format": "int64",
                "description": "Lifetime expired count."
              },
              "leases_failed": {
                "type": "integer",
                "format": "int64",
                "description": "Lifetime failed count."
              },
              "leases_succeeded": {
                "type": "integer",
                "format": "int64",
                "description": "Lifetime successful count."
              },
              "selftest": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/SelftestReport",
                    "description": "Latest complete self-test report."
                  }
                ]
              },
              "selftest_completed_at": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "int64",
                "description": "Latest self-test completion instant."
              },
              "selftest_requested_at": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "int64",
                "description": "Outstanding self-test request instant."
              }
            }
          }
        ],
        "description": "One runner with its live and lifetime counters."
      },
      "RunnerEventItem": {
        "type": "object",
        "description": "One row of runner history.",
        "required": [
          "id",
          "runner_id",
          "event_type",
          "occurred_at",
          "metadata"
        ],
        "properties": {
          "event_type": {
            "$ref": "#/components/schemas/RunnerEventType",
            "description": "What happened."
          },
          "id": {
            "type": "string",
            "description": "The row's identifier."
          },
          "metadata": {
            "description": "Event-specific detail. Free-form by design."
          },
          "occurred_at": {
            "type": "integer",
            "format": "int64",
            "description": "When, in epoch milliseconds."
          },
          "runner_id": {
            "type": "string",
            "description": "The runner it describes."
          }
        },
        "additionalProperties": false
      },
      "RunnerEventType": {
        "type": "string",
        "description": "Append-only runner history values.",
        "enum": [
          "runner_registered",
          "runner_online",
          "runner_offline",
          "lease_acquired",
          "lease_released",
          "runner_cordoned",
          "runner_draining",
          "runner_drained",
          "runner_revoked",
          "runner_token_rotated",
          "runner_policy_assigned"
        ]
      },
      "RunnerEventsResponse": {
        "type": "object",
        "description": "A page of runner history.",
        "required": [
          "items",
          "total"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RunnerEventItem"
            },
            "description": "The rows in this page."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Cursor for the next page, or null at the end."
          },
          "total": {
            "type": "integer",
            "format": "int64",
            "description": "Total rows matching the query."
          }
        },
        "additionalProperties": false
      },
      "RunnerItem": {
        "type": "object",
        "description": "One runner in the newest-first operator list.",
        "required": [
          "id",
          "host_id",
          "sandbox_tier",
          "admin_state",
          "liveness",
          "labels",
          "last_seen_at",
          "created_at",
          "degraded"
        ],
        "properties": {
          "achievable": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/CapabilityReport",
                "description": "Capability report most recently supplied by the host."
              }
            ]
          },
          "admin_state": {
            "$ref": "#/components/schemas/AdminState",
            "description": "Operator-controlled admission state."
          },
          "assigned_policy": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/AssignedPolicy",
                "description": "Policy currently assigned to the host."
              }
            ]
          },
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "Enrolment instant in epoch milliseconds."
          },
          "degraded": {
            "type": "boolean",
            "description": "Whether the assignment exceeds the reported capability."
          },
          "degraded_reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Stored explanation for a degraded verdict."
          },
          "host_id": {
            "type": "string",
            "description": "Stable host identity supplied at enrolment."
          },
          "id": {
            "type": "string",
            "description": "Canonical runner identifier."
          },
          "labels": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Placement labels."
          },
          "last_seen_at": {
            "type": "integer",
            "format": "int64",
            "description": "Last heartbeat instant in epoch milliseconds."
          },
          "liveness": {
            "$ref": "#/components/schemas/RunnerLiveness",
            "description": "Runtime state derived from heartbeat and leases."
          },
          "sandbox_tier": {
            "type": "string",
            "description": "Assigned isolation tier spelling."
          }
        },
        "additionalProperties": false
      },
      "RunnerLiveness": {
        "type": "string",
        "description": "Derived runtime liveness, computed by the fleet read and NEVER stored —\nstoring it would drift from the values it is derived from.",
        "enum": [
          "registered",
          "busy",
          "online",
          "offline"
        ]
      },
      "RunnersResponse": {
        "type": "object",
        "description": "A keyset page of runner list rows.",
        "required": [
          "items",
          "total"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RunnerItem"
            },
            "description": "Rows in newest-first order."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Cursor to send as `starting_after` on the next request."
          },
          "total": {
            "type": "integer",
            "format": "int64",
            "description": "Total runner rows independent of the boundary."
          }
        },
        "additionalProperties": false
      },
      "SandboxTier": {
        "type": "string",
        "description": "The isolation strength assigned to a runner.",
        "enum": [
          "landlock_full",
          "container_nested",
          "dev_none"
        ]
      },
      "SecretSummary": {
        "type": "object",
        "description": "One credential as the list shows it.",
        "required": [
          "name",
          "created_at",
          "kind"
        ],
        "properties": {
          "base_url": {
            "type": [
              "string",
              "null"
            ],
            "description": "The custom endpoint, where one may be displayed."
          },
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was first stored, in epoch milliseconds."
          },
          "kind": {
            "type": "string",
            "description": "The server's classification. A client reads this and never re-derives it."
          },
          "name": {
            "type": "string",
            "description": "The name the secret is stored and interpolated under."
          },
          "provider": {
            "type": [
              "string",
              "null"
            ],
            "description": "The provider label, for the kinds that carry one."
          }
        }
      },
      "SecretsResponse": {
        "type": "object",
        "description": "`GET /v1/workspaces/{workspace_id}/secrets` — every secret the workspace holds.",
        "required": [
          "secrets"
        ],
        "properties": {
          "secrets": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SecretSummary"
            },
            "description": "The rows, by name."
          }
        }
      },
      "SelfResponse": {
        "type": "object",
        "description": "`GET /v1/runners/me` reply — the runner's own registration row, read-only.\n\nReading this does NOT bump liveness, so inspecting a host can never mask a\ndead runner.",
        "required": [
          "id",
          "status",
          "host_id",
          "sandbox_tier",
          "last_seen_at",
          "degraded"
        ],
        "properties": {
          "achievable": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/CapabilityReport",
                "description": "What the host reported it can actually enforce."
              }
            ]
          },
          "assigned_policy": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/AssignedPolicy",
                "description": "The policy currently assigned to it."
              }
            ]
          },
          "degraded": {
            "type": "boolean",
            "description": "Whether the row reads degraded."
          },
          "degraded_reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why it reads degraded."
          },
          "host_id": {
            "type": "string",
            "description": "The host it runs on."
          },
          "id": {
            "type": "string",
            "description": "The runner's identifier."
          },
          "last_seen_at": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch milliseconds of the last beat; zero when never seen."
          },
          "sandbox_tier": {
            "type": "string",
            "description": "The tier it was assigned."
          },
          "status": {
            "type": "string",
            "description": "Operator-facing state."
          }
        },
        "additionalProperties": false
      },
      "SelftestCheck": {
        "type": "object",
        "description": "One self-test check's verdict.\n\n`detail` is prose even when `ok`: every passing check carries a line, and a\nwhitespace-free cause reads to an operator as a leaked internal identifier.",
        "required": [
          "name",
          "ok",
          "detail"
        ],
        "properties": {
          "detail": {
            "type": "string",
            "description": "Why, in prose."
          },
          "name": {
            "type": "string",
            "description": "What was checked."
          },
          "ok": {
            "type": "boolean",
            "description": "Whether it passed."
          }
        }
      },
      "SelftestReport": {
        "type": "object",
        "description": "One probe run and the verdict it reached.\n\nThe tier and policy travel with the verdict. Compare them against the\nrunner's current values to tell a stale result from a live one.",
        "required": [
          "checks",
          "all_ok",
          "sandbox_tier",
          "network_policy"
        ],
        "properties": {
          "all_ok": {
            "type": "boolean",
            "description": "Whether every check passed."
          },
          "checks": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SelftestCheck"
            },
            "description": "Every check the probe ran."
          },
          "network_policy": {
            "type": "string",
            "description": "The egress posture in force when the probe ran."
          },
          "sandbox_tier": {
            "type": "string",
            "description": "The tier in force when the probe ran."
          }
        }
      },
      "SteerAccepted": {
        "type": "object",
        "description": "What a steer returns once agentsfleet accepts the request.\n\nThe response carries the id the run is found under. Filter the live event\ntail on `event_id` to follow the message you just sent.",
        "required": [
          "status",
          "event_id"
        ],
        "properties": {
          "event_id": {
            "type": "string",
            "description": "The canonical event id the steer became."
          },
          "status": {
            "type": "string",
            "description": "Always `accepted`. A field rather than an implied 202, because that is\nwhat the daemon this ports writes."
          }
        }
      },
      "StoreSecretRequest": {
        "type": "object",
        "description": "Stores one secret under a name you choose.\n\nagentsfleet ignores fields it does not know, instead of refusing the\nrequest.",
        "required": [
          "name",
          "data"
        ],
        "properties": {
          "data": {
            "type": "object",
            "description": "The object to seal. agentsfleet reads nothing inside it beyond its\nshape.\n\nSend any well-formed JSON object."
          },
          "name": {
            "type": "string",
            "description": "The name a fleet interpolates as `${secrets.<name>.<field>}`."
          }
        }
      },
      "StoredModelEntry": {
        "type": "object",
        "description": "What a create and a model change both answer with.\n\nOne shape for both verbs because both say the same thing — which entry now\nstands — and the status code is what distinguishes them.",
        "required": [
          "id",
          "model_id",
          "secret_ref",
          "created_at"
        ],
        "properties": {
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was first stored, in epoch milliseconds."
          },
          "id": {
            "type": "string",
            "description": "The entry's identity."
          },
          "model_id": {
            "type": "string",
            "description": "The model it configures."
          },
          "secret_ref": {
            "type": "string",
            "description": "The vault key name backing it."
          }
        }
      },
      "StoredSecretResponse": {
        "type": "object",
        "description": "What a create and a replace both answer with.\n\nOne shape for both verbs because both say the same thing — which name now\nholds a secret — and the status code is what distinguishes them.",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "The name the secret is stored under."
          }
        }
      },
      "TenantProviderRequest": {
        "type": "object",
        "description": "`PUT /v1/tenants/me/provider`.",
        "required": [
          "mode"
        ],
        "properties": {
          "mode": {
            "$ref": "#/components/schemas/ProviderMode",
            "description": "Whose key to dial with."
          },
          "model": {
            "type": [
              "string",
              "null"
            ],
            "description": "The model to select, if the tenant is changing it."
          },
          "secret_ref": {
            "type": [
              "string",
              "null"
            ],
            "description": "The credential to dial with, required under self-managed mode."
          }
        },
        "additionalProperties": false
      },
      "TenantProviderResponse": {
        "type": "object",
        "description": "`GET /v1/tenants/me/provider`, and what a write answers with.",
        "required": [
          "mode",
          "provider",
          "model",
          "context_cap_tokens",
          "platform_default_available"
        ],
        "properties": {
          "context_cap_tokens": {
            "type": "integer",
            "format": "int32",
            "description": "The context window the catalogue prices this model at.",
            "minimum": 0
          },
          "mode": {
            "$ref": "#/components/schemas/ProviderMode",
            "description": "Whose key this tenant's runs dial with."
          },
          "model": {
            "type": "string",
            "description": "The model identifier as the provider spells it."
          },
          "platform_default_available": {
            "type": "boolean",
            "description": "Whether an active platform-default row exists at all."
          },
          "provider": {
            "type": "string",
            "description": "The provider the model belongs to."
          },
          "secret_ref": {
            "type": [
              "string",
              "null"
            ],
            "description": "The credential dialled under self-managed mode."
          }
        }
      },
      "ThreadResponse": {
        "type": "object",
        "description": "A page of one fleet's chat thread, bodies included.",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EventDetail"
            },
            "description": "The turns on this page, newest first."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the next page resumes, or `null` when this one ends the walk."
          },
          "total": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "How many turns the thread holds in total. Never populated.",
            "minimum": 0
          }
        }
      },
      "ToolCallCompleted": {
        "type": "object",
        "description": "A tool call finished.",
        "required": [
          "name",
          "ms"
        ],
        "properties": {
          "ms": {
            "type": "integer",
            "format": "int64",
            "description": "How long it took, in milliseconds."
          },
          "name": {
            "type": "string",
            "description": "Which tool."
          }
        },
        "additionalProperties": false
      },
      "ToolCallProgress": {
        "type": "object",
        "description": "A long-running tool is still working, so a reader's spinner survives it.",
        "required": [
          "name",
          "elapsed_ms"
        ],
        "properties": {
          "elapsed_ms": {
            "type": "integer",
            "format": "int64",
            "description": "How long it has been running, in milliseconds."
          },
          "name": {
            "type": "string",
            "description": "Which tool."
          }
        },
        "additionalProperties": false
      },
      "ToolCallStarted": {
        "type": "object",
        "description": "A tool call began.\n\n`args_redacted` is opaque, pre-stringified JSON built runner-side AFTER\nsubstitution — never the resolved bytes.",
        "required": [
          "name",
          "args_redacted"
        ],
        "properties": {
          "args_redacted": {
            "type": "string",
            "description": "The redacted arguments."
          },
          "name": {
            "type": "string",
            "description": "Which tool."
          }
        },
        "additionalProperties": false
      },
      "UpdateModelEntryRequest": {
        "type": "object",
        "description": "`PATCH /v1/tenants/me/models/{id}` — point an entry at another model.",
        "required": [
          "model_id"
        ],
        "properties": {
          "model_id": {
            "type": "string",
            "description": "The model to point at."
          }
        }
      },
      "VerifySessionRequest": {
        "type": "object",
        "description": "`POST /v1/auth/sessions/{session_id}/verify` — the code is presented.",
        "required": [
          "verification_code"
        ],
        "properties": {
          "verification_code": {
            "type": "string",
            "description": "The six digits a person read out of the browser."
          }
        },
        "additionalProperties": false
      },
      "VerifySessionResponse": {
        "type": "object",
        "description": "What a redeemed login hands back.",
        "required": [
          "dashboard_public_key",
          "ciphertext",
          "nonce"
        ],
        "properties": {
          "ciphertext": {
            "type": "string",
            "description": "The sealed credential."
          },
          "dashboard_public_key": {
            "type": "string",
            "description": "The dashboard's public key."
          },
          "nonce": {
            "type": "string",
            "description": "The nonce it was sealed under."
          }
        }
      },
      "View": {
        "type": "object",
        "description": "One schedule, as the tenant surface renders it.",
        "required": [
          "schedule_id",
          "fleet_id",
          "cron",
          "timezone",
          "message",
          "status",
          "sync",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was created."
          },
          "cron": {
            "type": "string",
            "description": "The expression it fires on."
          },
          "fleet_id": {
            "type": "string",
            "description": "The fleet it wakes."
          },
          "last_error": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why the last push failed, when one did."
          },
          "message": {
            "type": "string",
            "description": "What the fleet is asked to do."
          },
          "schedule_id": {
            "type": "string",
            "description": "Its identity."
          },
          "status": {
            "type": "string",
            "description": "What the operator wants it to be doing."
          },
          "sync": {
            "type": "string",
            "description": "How far agentsfleet has brought the external scheduler in line.\n\nA schedule can save without registering. This field shows you that\nstate."
          },
          "timezone": {
            "type": "string",
            "description": "The zone that expression is read in."
          },
          "updated_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was last changed."
          }
        }
      },
      "WebhookUrl": {
        "type": "object",
        "description": "Where one declared webhook trigger is delivered.",
        "required": [
          "source",
          "url"
        ],
        "properties": {
          "source": {
            "type": "string",
            "description": "The provider that sends it."
          },
          "url": {
            "type": "string",
            "description": "The address it posts to, on this deployment."
          }
        }
      },
      "WorkspaceSummary": {
        "type": "object",
        "description": "One workspace as the list shows it.",
        "required": [
          "id",
          "created_at"
        ],
        "properties": {
          "created_at": {
            "type": "integer",
            "format": "int64",
            "description": "When it was created; the walk's sort key."
          },
          "id": {
            "type": "string",
            "description": "The workspace's identifier."
          },
          "name": {
            "type": [
              "string",
              "null"
            ],
            "description": "Its name — `null` on rows older than naming, emitted either way for\nthe tenant module's null rule."
          }
        }
      },
      "WorkspacesResponse": {
        "type": "object",
        "description": "`GET /v1/tenants/me/workspaces` — one page of the tenant's workspaces.",
        "required": [
          "items",
          "tenant_id"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WorkspaceSummary"
            },
            "description": "The rows on this page, oldest first."
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the next page resumes, or `null` on the last page."
          },
          "tenant_id": {
            "type": "string",
            "description": "Whose list this is — the authoritative resolution, carried so a client\ncan pin its local state to the right tenant."
          },
          "total": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Always `null`: the walk never counts, and the key stays because a\nclient may already branch on its presence."
          }
        }
      }
    },
    "securitySchemes": {
      "BearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT",
        "description": "Obtain a token via the CLI auth flow (POST /v1/auth/sessions) or GitHub OAuth"
      }
    }
  }
}
