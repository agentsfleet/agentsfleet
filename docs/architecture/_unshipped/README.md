# _unshipped/

Architecture documents that depend on a v2 launch event (date, first external install, deferral evidence) that has not yet happened. Held here rather than in the canonical architecture set so the directory does not advertise placeholder content as canon.

When v2 ships and the placeholders can be filled with real evidence, move the file back to `docs/architecture/` and update the parent `README.md` table of contents.
