{
  "name": "@agentsfleet/cli",
  "version": "0.26.2",
  "description": "agentsfleet CLI",
  "repository": {
    "type": "git",
    "url": "https://github.com/agentsfleet/agentsfleet"
  },
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "agentsfleet": "./dist/bin/agentsfleet.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=24.0.0",
    "bun": ">=1.3.14"
  },
  "scripts": {
    "test": "bun run build && bun ./scripts/enforce-coverage.mjs",
    "test:acceptance": "npm run build && bun run test:acceptance:deterministic:run && bun run test:acceptance:live:run",
    "test:acceptance:deterministic": "npm run build && bun run test:acceptance:deterministic:run",
    "test:acceptance:deterministic:run": "bun test/acceptance/run-lane.ts deterministic",
    "test:acceptance:live": "npm run build && bun run test:acceptance:live:run",
    "test:acceptance:live:run": "bun test/acceptance/run-lane.ts live",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test scripts && node ./scripts/audit-runtime-imports.mjs && node ./scripts/audit-const-names.mjs && tsc --noEmit",
    "lint:fix": "oxlint src test scripts --fix && node ./scripts/audit-runtime-imports.mjs && node ./scripts/audit-const-names.mjs && tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@vercel/detect-agent": "^1.2.4",
    "commander": "^15.0.0",
    "effect": "4.0.0-rc.108",
    "posthog-node": "^5.47.10",
    "uuid": "^14.0.1"
  },
  "license": "MIT",
  "devDependencies": {
    "@clerk/testing": "^2.2.17",
    "@playwright/test": "1.62.1",
    "@types/node": "^26.1.2",
    "@types/uuid": "^11.0.0",
    "bun-types": "^1.3.14",
    "oxlint": "^1.77.0",
    "playwright": "^1.62.1",
    "typescript": "7.0.2"
  }
}
