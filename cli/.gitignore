# Agent skills moved to github.com/agentsfleet/skills — no longer bundled
# into the CLI package. `/skills/` stays as defense-in-depth against stray
# dev-time dirs.
/skills/

# Per-suite minted Clerk session JWT (mode 0600). Written by
# test/acceptance/global-setup.js; never published.
/test/acceptance/.fixture-jwt
