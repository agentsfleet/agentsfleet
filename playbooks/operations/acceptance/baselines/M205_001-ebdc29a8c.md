# M205_001 test baseline — comparison revision `ebdc29a8c0a42ed4aa963b9e532cfee61f5995a1`

## Environment

macOS 27.0, Rust workspace `0.49.0`, Zig 0.16.0, docker compose
`postgres:18-alpine` and `dragonfly:v2.0.0` on worktree-derived ports, schemas
reset per run (`KEEP_TEST_STATE` unset). Measured in the stream's own worktree
at `/Users/kishore/Projects/agentsfleet-m205-constant-ties`, with the root,
`cli/` and every `ui/packages/*` install already present.

## Branch — measured

| Lane | Command | Result |
|---|---|---|
| unit (Rust) | `make test-unit-all` | **2637 passed, 0 failed**, exit 0 |
| unit (cli) | same lane | **1669 pass, 0 fail, 16 skip** — 1685 across 189 files |
| unit (app) | same lane | **2885 passed** |
| unit (design-system) | same lane | **631 passed** |
| unit (website) | same lane | **142 passed** |
| unit (Zig runner) | `zig build test --build-file build_runner.zig` | **721 pass, 3 skip** (724), 9/9 steps, exit 0 |
| integration | `make test-integration-rustd` | **561 passed, 0 failed**, exit 0 |
| lint | `make lint-all` | exit 0 |
| version | `make check-version` | exit 0 — all versions match `0.49.0` |
| secrets | `gitleaks detect` | exit 0 — 5828 commits, 196.23 MB, no leaks |
| cross-compile | `zig build --build-file build_runner.zig -Dtarget={x86_64,aarch64}-linux-musl` | exit 0 each |

## Comparison revision — derived, not measured

A baseline worktree needs a full Rust build. The machine had **19 GiB free**
against a `rustd/target` of 23 GiB, 34 GiB and 5.5 GiB across the three live
worktrees, so a fourth checkout could not be built. The figures below are
arithmetic on the measured branch counts less this diff's test delta, counted
from `git diff origin/main...HEAD` by test-function name.

| Lane | Branch | Delta | Baseline |
|---|---|---|---|
| unit (Rust) | 2637 | **−3** | **2640** |
| unit (Zig) | 721 | **+5** | **716** |
| unit (cli) | 1669 | **+1** | **1668** |
| unit (app) | 2885 | **+4** | **2881** |
| unit (design-system) | 631 | 0 | 631 |
| unit (website) | 142 | 0 | 142 |
| integration | 561 | 0 | 561 |

Counting used `[[:space:]]*`, not `\s`: `git grep -E` is POSIX ERE and matches
`\s` as a literal `s`, which silently counts only unindented attributes.

## Why the Rust unit count falls

**Rust unit −3 is this milestone's deliverable, not a coverage loss.** All
seven removed tests are the two deleted pin files, and nothing else lost one:

| File | Δ | Note |
|---|---|---|
| `afd_billing/tests/cross_runtime_rates.rs` | **−5** | the TypeScript parser, deleted |
| `afd_core/tests/cross_runtime_timing.rs` | **−2** | the Zig parser, deleted |
| `afd_api_runner/…/heartbeat.rs` | +2 | Dimension 2.4 |
| `afd_core/src/money.rs` | +2 | the wire unit's one Rust home |

Both deleted files failed OPEN: a parser that stops matching exits zero, so the
suite reported green while the guarantee was already gone. The nine constants
they policed are now single-declared or carried on the wire. Net across every
runtime is **+7**.

## Zig proofs run by hand

No declared lane runs Zig tests — `make test-unit-all` is
`test-unit-rustd + test-coverage-all`, `make dry` is Playwright, and the only
Zig in any lane is `zig fmt --check`. `grep -rn 'zig build test'` across the
workflows, `make/`, `Makefile` and `scripts/` returns nothing. Dimensions 2.1,
2.2, 2.3 and 2.5 were therefore graded from the manual run recorded above, and
the gap is its own milestone rather than a footnote here.
