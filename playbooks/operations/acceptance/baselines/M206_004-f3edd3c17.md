# M206_004 test baseline — comparison revision `f3edd3c17062087a6db7f7e271606bf0c3901259`

Measured in a detached worktree at the exact comparison revision, before the
Pull Request. Both declared baseline lanes exited 0.

## Environment

macOS 27.0 arm64; Rust workspace version `0.49.0`. The isolated worktree was
`/Users/kishore/Projects/agentsfleet-m206-004-ci-incident-drill/.baseline-m206-004`.
Root, `cli/`, and each `ui/packages/*` dependency install were completed before
testing. Integration used its own Docker Compose Postgres, Dragonfly, and
QStash project (`baseline-m206-004`), with schemas reset for that run; it did
not share the implementation branch's integration datastores.

## Measured results

| Lane | Command | Passed | Failed | Skipped or ignored |
|---|---|---:|---:|---:|
| unit — Rust workspace | `make test-unit-all` | 2662 | 0 | 592 ignored |
| unit — app | same | 2976 | 0 | 0 |
| unit — website | same | 142 | 0 | 0 |
| unit — CLI | same | 1755 | 0 | 16 skipped |
| unit — design system | same | 634 | 0 | 0 |
| integration — Rust workspace | `make test-integration-rustd` | 572 | 0 | 0 |

The unit command printed `All unit lanes passed`. The integration total is
571 tests in the standard suite plus one in the exclusive suite. Rust counts
sum Cargo's `test result:` lines; the other counts are each package runner's
reported totals. The local `baseline-unit.log` and
`baseline-integration.log` were removed with the isolated worktree after the
counts were recorded; this report is the durable evidence.

The branch's final results and selection changes belong in the Pull Request
evidence, after its integration lane finishes. This report preserves only the
historical baseline.
