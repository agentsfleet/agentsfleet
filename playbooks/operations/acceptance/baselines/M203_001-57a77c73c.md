# M203_001 test baseline — comparison revision `57a77c73cdfe30b66cc1a765da91a64406fb37f0`

## Environment

macOS 27.0, Rust workspace `0.49.0`, docker compose `postgres:18-alpine` and
`dragonfly:v2.0.0` on worktree-derived ports, schemas reset per run
(`KEEP_TEST_STATE` unset). Baseline measured in a detached worktree at the
comparison revision, with its own `bun install` at the root, in `cli/`, and in
each `ui/packages/*` package.

## Comparison revision — measured

| Lane | Command | Result |
|---|---|---|
| unit | `make test-unit-all` | **2644 Rust passed, 0 failed**, exit 0 |
| unit (TypeScript) | same lane | app 2871 passed · design-system 142 · website 631 |
| integration | `make test-integration-rustd` | **550 — DERIVED, see below** |

## Why the integration baseline is derived, not measured

The baseline lane's own build exhausted the disk before it could run:

```
ld: write() failed, errno=28 (No space left on device)
/dev/disk3s5  228Gi  201Gi  125Mi  100%
```

The figure is therefore arithmetic on two measured quantities rather than a
reading: the branch's measured **561 passed / 0 failed**, less the **11**
integration tests this diff adds and the **0** it removes, both counted from
`git diff origin/main...HEAD` by test-function name. It agrees with the figure
the stream's own checkpoint recorded independently.

A prior session's `baseline-integration.log` reads 512 and is NOT this
baseline: it was taken in a worktree pinned at `5eb6f3885`, **59 commits
behind** this comparison revision and predating the M200 and M201 merges.
Citing it would have credited M203 with roughly +49 integration tests it did
not write.

## Branch — measured

| Lane | Command | Result |
|---|---|---|
| unit | `make test-unit-all` | **2648 Rust passed, 0 failed**, exit 0 |
| unit (TypeScript) | same lane | app 2870 passed · design-system 142 · website 631 |
| integration | `make test-integration-rustd` | **561 passed, 0 failed**, exit 0 |
| lint | `make lint-all` | exit 0 |
| version | `make check-version` | exit 0 — all versions match `0.49.0` |

## Delta

**Rust unit +4. Integration +11. TypeScript app −1.**

The TypeScript fall is deliberate. Three proofs asserted a charge carrying
`fleet_id: null`, which slot 916 made impossible by making the column
`NOT NULL`; two replacements assert the same two arms against
`agentDisplayName`, whose `fleetId: string | null` a lease still reaches
through `AgentLabel`'s prop. Coverage held at 100% across statements,
branches, functions and lines, so no branch lost its proof — a duplicate went
with them.

## Coverage

TypeScript 100% — statements 6708/6708, branches 4056/4056, functions
1809/1809, lines 5999/5999. Rust diff files: line coverage 100% on twelve of
fourteen and 98.91% on `afd_fleet/src/lease/event.rs`, functions 100%
throughout, measured with both tiers instrumented (`--include-ignored`, 3104
tests). Zero uncovered lines among the lines this diff adds or changes.
