# M194_001 — test baseline and delta

Comparison revision `3fbf3d9c3823463bd99a2998b18eb52d0ac14ded`, measured in the
sibling worktree `agentsfleet-m194-acceptance-walk-verdict` checked out at that
exact commit, so the default checkout's drift cannot contaminate it. The final
lanes ran in `agentsfleet` on `chore/m194-acceptance-walk-close`. Separate
compose projects, so neither lane reset the other's datastore.

| Lane | Command | Baseline @ `3fbf3d9c3` | Final @ branch | Delta |
|---|---|---|---|---|
| unit — Rust | `make test-unit-all` | 2500 passed, 0 failed, 448 ignored | 2499 passed, 0 failed, 448 ignored | **−1** |
| unit — TypeScript | (same target) | 2708 + 146 + 560 = 3414 | 2713 + 146 + 560 = 3419 | **+5** |
| integration | `make test-integration-rustd` | 445 passed, 0 failed | 445 passed, 0 failed | 0 |

Both baseline lanes exit 0. Both final lanes exit 0.

## The Rust −1, which is not a regression

Four movements net to −1, and each is deliberate:

- **−2** the two Zig-parity tests retired on Indy's call, `should_declare_only_codes_the_zig_registry_also_declares` and `test_entries_match_the_zig_registry`. Their frozen lists compared this registry against a daemon tree deleted in `2f0021d1b`; nothing but their own tests read either list.
- **+2** `a_misnamed_credential_answers_the_code_whose_remedy_is_renaming_it` pins the `UZ-BUNDLE-006` arm and the ordering that keeps it ahead of the bundle catch-all; `test_a_misnamed_credential_is_refused_by_the_code_that_says_rename_it` drives a real `TRIGGER.md` through `prepare()` so the parser's output is proven to reach that arm.
- **−1** `test_migration_list_matches_schema_directory_and_zig` became `test_migration_list_matches_schema_directory`; the third comparison it made was against `schema/embed.zig`, deleted here.

Coverage retired with the parity lists: status, title and dashboard prose for
130 `Problem` entries are no longer pinned byte-for-byte. Still asserted —
entry totality over `REGISTRY` in both directions, the derived documentation
link, the eight auth-plane statuses, code grammar, and uniqueness.

## Red-green, per added test

| Mutation | Result |
|---|---|
| delete the `InvalidCredentialRef` arm | does not compile — `-D warnings` on the now-unused import |
| pattern → `InvalidVersion` (compiles) | `FAILED` — `left: ErrorCode("UZ-BUNDLE-001")  right: ErrorCode("UZ-BUNDLE-006")` |
| re-wrap the parser refusal at `frontmatter.rs:34` | classification test still `ok`; **seam test `FAILED`** — the gap the seam test exists to close |
