# M206_001 test baseline — comparison revision `f3edd3c17062087a6db7f7e271606bf0c3901259`

One baseline for the branch `feat/m206-slack-incident-responder`, which carries
M206_001 and the two workstreams folded into it, M206_002 and M206_003.

## Environment

macOS 27.0, Rust workspace `0.49.0`, docker compose `postgres:18-alpine` and a
four-node `dragonfly:v2.0.0` cluster on worktree-derived ports, schemas reset
per run (`KEEP_TEST_STATE` unset).

## Comparison revision — measured

Measured on an isolated checkout of the comparison revision: a detached
worktree `agentsfleet-m206-baseline` at `f3edd3c17`, with `bun install` at the
root, in `cli/` (plus `bun run build`), and in each `ui/packages/*` package.
The worktree had its own compose project and ports, so neither run could reset
the branch's datastores or another stream's. Its Rust build shared the branch
worktree's `CARGO_TARGET_DIR` for third-party crates only; workspace crates
rebuild per checkout path. Run Sep 24, 2026, 14:39–14:55 IST. The worktree and
its compose project were removed afterwards.

`f3edd3c17` is four documentation-only commits above `origin/main`
(`b1bc6f0c4`), so these figures are also `main`'s.

| Lane | Command | Result |
|---|---|---|
| unit (Rust) | `make test-unit-all` → `cargo test --workspace --all-features` | **2662 passed, 0 failed, 592 ignored** across 154 binaries, exit 0 |
| unit (TypeScript) | same lane, the coverage gates | app 2976 (314 files) · website 142 (22 files) · cli 1755 pass / 0 fail · design-system 634 (59 files) |
| integration | `make test-integration-rustd` | **571 passed** in the parallel suite + **1 passed** in the exclusive suite = **572, 0 failed**, exit 0 |

## Branch — measured at close

Recorded from the `orly gate pr` run that opens the Pull Request; see that
Pull Request's Session notes for the command output.
