# M199_001 test baseline — `4079743cb7f760b2b4ae45c877efbf062c9a7618`

Measured before the Pull Request, on the recorded comparison revision, in a
detached worktree at `/private/tmp/.../scratchpad/baseline`.

## What was measured, and what was not

The diff is TypeScript under `cli/` alone — no Rust, no Zig, no `ui/**`, no
schema. So the comparison that carries information is the Command-Line
Interface (CLI) suite, measured on both sides. The Rust half of
`make test-unit-all` is identical by construction (the branch changes no file
it compiles) and is reported once, from the branch.

The integration lane (`make test-integration-rustd`) exercises `rustd` against
live Postgres and Dragonfly. This branch changes no file that lane loads, so it
is reported from the branch alone rather than measured twice.

## Baseline — `4079743cb`

| Lane | Command | Result |
|---|---|---|
| CLI unit | `cd cli && bun test` | **1673 passed · 14 skipped · 0 failed** — 1687 tests across 169 files |

A first attempt reported 158 failures. The cause was the baseline worktree
having no built `dist/`: the specs that spawn the published binary got
`node:internal/modules/cjs/loader` instead of a CLI. `bun run build` first, then
re-measure — the number above is the second run, and the first is recorded here
so a future reader does not rediscover it.

## Branch — `feat/m199-cli-library-approvals-consistency`

| Lane | Command | Result |
|---|---|---|
| CLI unit | `cd cli && bun test` | **1748 passed · 16 skipped · 0 failed** — 1764 tests across 177 files |
| CLI coverage gate | `cd cli && bun run test` | **PASS** — line 100.00% against a 100.00% floor |
| Repository unit | `make test-unit-all` | **exit 0** — Rust 2630 passed / 0 failed; every TypeScript package coverage gate green |
| Lint | `make lint-all` | exit 0 |
| Deterministic acceptance | `cd cli && bun run test:acceptance:deterministic` | **213 passed · 0 failed** across 11 files |

## Test delta

**+75 passing, +77 tests, +8 files.** Positive growth on a code-adding diff, as
the Test Delta row requires.

## The live acceptance lane

`cd cli && bun run test:acceptance:live` is not run here. It mints a session
through Clerk and requires `CLERK_SECRET_KEY` resolved from the vault at the
workflow layer; resolving a credential at an operator's terminal is forbidden,
so the lane runs in Continuous Integration (CI).

The journeys those specs walk were instead exercised by hand against
`https://api-dev.agentsfleet.net`, with the output recorded in the Pull Request
Session Notes: both onboarding source kinds, the gallery listing that follows,
an install from a tenant identifier, a gate read and decided from the terminal,
and a parked steer naming the gate that held it.
