# Acceptance visual verdict

- Build: ff2fd5ee573ab25b4bab20568909aed507c590fc
- Reviewer: nkishore@megam.io
- Date: 2026-09-08
- Verdict: fail
- Defects: Step 4 — the operator's message is accepted and never runs. The turn appears at once, the fleet reads `Still working.`, and no assistant turn arrives; at 4 minutes (allowance is 2) TOKENS and DURATION are still `—`. Fleet events carry one row, `RECEIVED`, `No result recorded`, never leased. The runner `zombie-dev-worker-ant` is `ACTIVE · ONLINE`, heartbeat 5 seconds, `Idle. No active leases.`, all eight sandbox checks passed — so the playbook's runner-offline branch does not apply. Its newest lease of any kind is 1 day old. Steps 5, 6 and 7 were not walked: the playbook stops the walk at a failed step. The daemon log names the mechanism: `runner_lease_no_work` on every poll, and `assign_pel_redelivered fleet_id=01a0804a-6cf3-7a3b-aec7-dab74abb7b2a` — the delivery is claimed, never acknowledged, and redelivered forever.
- Defects: Step 4 (second) — the composer is disabled while the fleet reads `Still working.`, so a second message is silently dropped. Fleet events still show one event after the second send. A first message that wedges the fleet leaves the operator no way to send another.
- Defects: Zoho Desk, Jira and Linear — **stale registrations, all three.** Each app held the retired `https://api-dev.agentsfleet.net/v1/connectors/<provider>/callback`. The reviewer repaired all three during the walk by registering `https://app-dev.agentsfleet.net/api/connectors/<provider>/callback` — in each provider's App settings, never in code. Linear then connected. Zoho Desk and Jira are registered but deliberately not authorized: this walk classifies them and stops at the consent screen. Only Linear announced itself, with `redirect_url not found`; Zoho and Jira showed sign-in pages, which is why the playbook now has a row for that.
- Defects: Linear — **stale registration.** The provider answers `redirect_url not found`, which is wording naming the redirect. The daemon sent `redirect_uri=https://app-dev.agentsfleet.net/api/connectors/linear/callback`, which is what `rustd/crates/afd_connector/src/callback.rs:42` mints (`RELAY_PATH = ["api", "connectors"]` against the dashboard host). So the code is right and Linear's OAuth app held something else. **Repaired during the walk:** the reviewer added the `app-dev` callback in Linear's App settings — in the provider, never in code, exactly as this playbook prescribes — and the row now reads CONNECTED. The registration drifted once, so it is recorded here rather than quietly fixed.
- Defects: Integrations — `Connect` is intermittently inert. The click fires its server action (`POST /w/<id>/integrations` → 200) and the browser never navigates to the provider; no error, no state change, row stays `NOT CONNECTED`. Reproduced on Linear (every attempt), Jira and Zoho (later attempts), and on `Inspect runner →` on Admin → Runners, so it is not connector-specific.

Steps 1, 2 and 3 reached their stated observations. The GitHub connect completed —
the row reads `CONNECTED` and survives a reload — which is what M187_001's
`User-Agent` fix was for, and it is the first time this leg has ever completed.

Zoho and Jira were classified, not authorized: each reached its provider carrying
`redirect_uri=https://app-dev.agentsfleet.net/api/connectors/<provider>/callback`,
the value the daemon is supposed to mint, and neither showed wording naming the
redirect. Both landed on the provider's LOGIN page rather than its consent screen,
which this playbook's own §"Connector registrations" says cannot distinguish a good
registration from a stale one. Linear is classified: its registration is stale, recorded above with the provider's verbatim wording.
